# CP/M File Manager — Change History

> Companion to the [Software Requirements Specification](cpm_fm_requirements.md) (CPM-FM-SRS).
> This is the SRS §11 Change History, kept in a separate file so the main specification stays
> small. The application/SRS version (DR-040/DR-041) is updated with each entry; add new
> versions at the top of the table (see the SRS "Requirement-change workflow" in `AGENTS.md`).

| Version | Date | Author | Description |
|---------|------|--------|-------------|
| 2.36.0 | 2026-07-09 | Software Engineer | Config menu & window reorganisation. **Feature:** (1) a new **Config → Remote** menu item opens a new **Remote Config** dialog holding the settings split out of General Config — the remote-command fields (List Files, Receive/Send from Remote + their Test buttons, Use XMODEM-1K and the two 1K commands, Rename, Delete, Erase All), the three transfer-timing fields (Xfer Launch Delay / Handshake Timeout / Inter-file Delay), and the Boot Sequence editor — presented **ungrouped** (no "Remote" group box). (2) **End of Line** and **Echo Transfer Data** move from General Config to the **Terminal Config** dialog's Terminal tab. (3) All four config dialogs (Serial, General, Terminal, Remote) now wrap their fields in a **vertical scroll area** so tall dialogs stay fully reachable, with the Save/Cancel row fixed below. (4) **New Config / Load Config / Save Config** move from the **File** menu to the top of the **Config** menu (separated from the dialog items). After the split, General Config holds only Debug Logging, Viewer/Editor, Default Host Directory and Default Image Directory. Persisted setting keys are unchanged, so existing config files load unchanged; each dialog's Save persists only its own subset to the active file (Remote via the new FR-021d, EOL/Echo now via the Terminal subset FR-021c). **Added:** FR-021d (Remote dialog subset save), UIR-116 (Remote Config dialog), UIR-117 (config-dialog vertical scrollbars). **Modified:** UIR-002 (File menu no longer lists New/Load/Save Config), UIR-003 (Config menu adds New/Load/Save Config + Remote), UIR-041/UIR-044 (General dialog trimmed + ungrouped), UIR-047/UIR-048/UIR-058 (End of Line / Echo Transfer Data now on the Terminal tab), UIR-103a (Terminal tab lists the two moved fields), UIR-042/UIR-045/UIR-046/UIR-049/UIR-052/UIR-055/UIR-056/UIR-059/UIR-089/UIR-090/UIR-093/UIR-094/UIR-095/UIR-107 (re-homed to the Remote Config dialog, ungrouped), UIR-108 (Open Disk Image now the first File-menu item), FR-006/FR-010/FR-013/FR-018/FR-019 (New/Load/Save Config menu-path wording), FR-161 (Test buttons on the Remote dialog). **Code:** `gui/config_dialogs.py` (new `RemoteConfigDialog` with the moved fields + Test machinery; `GeneralConfigDialog` trimmed to four fields; `TerminalConfigDialog` gains the `eol`/`echo_transfer_data` fields; `ConfigDialog.create_widgets` and the Terminal tab wrapped in `QScrollArea` — UIR-117); `gui/mw_config.py` (`menu_remote_config` + subset save; `RemoteConfigDialog` import); `app.py:setup_menu` (New/Load/Save Config moved to the Config menu, new Config → Remote item, Open Disk Image now first in File); `lang/lang_*.txt` (rename the moved fields' label keys to `config.remote.*` and `config.terminal.eol`/`.echo_transfer`; add `menu.config.remote` and `config.remote.title`; remove `config.general.remote_group`; all 12 languages). **Tests:** `tests/test_gui_smoke.py` — replaced `test_general_config_remote_group_first` with `test_remote_config_fields_ungrouped` (Remote dialog field order, ungrouped, in a scroll area) and `test_general_config_trimmed_fields`; re-pointed the Test-button tests at the Remote dialog; moved the echo-field test to `test_terminal_config_has_eol_and_echo_transfer_fields`; re-pointed the XMODEM-1K and Boot Sequence round-trip tests at the Remote dialog; added `test_remote_config_save_persists_remote_only` (FR-021d); updated the General/Remote subset-save tests to use each dialog's actual fields. Full unit suite green (658 tests); `ruff`/`mypy` clean; views regenerated, 0 stale tags; FR-021d/UIR-116/UIR-117 and the moved field IDs covered. **Integration (HIL):** no change needed — the change is a host-side config-dialog/menu reorganisation with no protocol or GUI-over-serial effect; the HIL suite reads only the unchanged setting keys (`eol`, `list_files_cmd`, …) and asserts nothing about the config-dialog widget tree or the File/Config menu structure. **Docs:** user manual (Config menu, new Config → Remote section, General/Terminal sections, ToC, Reference: Default Settings) and manual test plan/scorecard (versioned) updated; `src/version.txt` / SRS version field / user-manual version line → `2.36.0`; SRS §9 row added. |
| 2.35.0 | 2026-07-09 | Software Engineer | CP/M disk image UX overhaul — five fixes. **(1) Host-directory change no longer disturbs a Remote-mounted image.** Change Directory / a General Config host-directory edit now discard the open image only when it is **Host-side** mounted (its working directory *is* the Host pane); a Remote-mounted image is independent of the host folder and stays put. **(2) Separate image directory.** A new `image_directory` setting (a **Default Image Directory** field in General Config, UIR-115) is tracked apart from the host directory: Open/New/Save Image browse it, it updates to the folder of the last image opened/saved, and File → Save Config persists it (FR-179). **(3) File-menu rename** — the first three items are now **New Config / Load Config / Save Config** (i18n only). **(4) KISS image saving.** The opt-in `image_write_enabled` gate is **removed** (setting, General Config checkbox, and all gating): disk-image writing is always available. Save Image now behaves like a normal document save — an image with a file is **overwritten in place** (no dialog, boot tracks preserved), a new/unnamed image is written via a one-time Save-As and then named after it. The Save/Discard/Cancel unsaved-changes guard (FR-175) is always active when dirty and now also protects a never-saved new image. **(5) Backup/Restore to images.** With an image mounted in the Remote pane, **Backup** and **Restore** are local bulk copies (host↔image): Backup wipes the host folder then copies every image file out; Restore wipes the image then copies every host file in (8.3-validated). Image↔remote Backup/Restore for a Host-side image + connected device already worked over the serial path (unchanged). **Added:** FR-179 (separate image directory), FR-180 (local Backup/Restore for a Remote-mounted image), UIR-115 (Default Image Directory field). **Modified:** FR-171/FR-176 (host-dir change discards only a Host-side image; Remote-side is host-dir-independent), FR-174 (KISS overwrite-in-place save; no opt-in), FR-178 (New Disk Image always available), UIR-002 (menu labels), UIR-110/UIR-114 (no opt-in gate), DR-050 (source overwrite now allowed), FR-150/FR-151 (Backup/Restore also mirror host↔image), §3.19 intro. **Removed:** the `image_write_enabled` setting/requirement. **Code:** `utils/config_handler.py` (drop `image_write_enabled`, add `image_directory`); `gui/config_dialogs.py` (replace the checkbox with the Default Image Directory field); `app.py` (`image_dir` state); `mw_config.py` (persist/load `image_dir`; host-dir edit gated on Host-side + follows an image-dir edit); `mw_file_panes.py:change_host_dir` (Host-side-only image discard); `mw_disk_image.py` (drop `_image_write_enabled`/`_update_new_image_action`; simplify `_update_save_image_action`; KISS `menu_save_image` overwrite-in-place; `menu_open_image`/`menu_save_image` use `image_dir`; guard drops the writing/source conditions); `mw_backup_restore.py` (`do_backup`/`do_restore` branch to `_backup_image_to_host`/`_restore_host_to_image` for a Remote-mounted image); `lang/lang_*.txt` (rename New/Load/Save → "… Config"; replace `config.general.image_write` with `config.general.image_directory`; all 12 languages). **Tests:** `tests/test_gui_smoke.py` — rewrote the writing-gate/Save-As-refusal tests for KISS (Save action enabled iff image open; overwrite-in-place; copy-to-image saved in place); added change-dir-keeps-remote-image, separate-image-directory persistence, and local Backup/Restore tests; a module autouse fixture defaults the unsaved-changes prompt to "discard" so headless closes of a dirty image don't hang. Full unit suite green; `ruff`/`mypy` clean; views regenerated, 0 stale tags; FR-179/FR-180/UIR-115 covered. **Integration (HIL):** no new automated case — the new paths are host-side (local copy / config); image↔remote Backup/Restore uses the already-HIL-covered serial path. New manual cases MT-DI17/MT-DI18/MT-DI19. **Docs:** user manual (disk-image sections + General Config reference) and manual test plan/scorecard (MT-DI17–19; MT-DI16 de-gated; versioned 1.47→1.48) updated; `src/version.txt` / SRS version field / user-manual version line → `2.35.0`; SRS §9 row added. |
| 2.34.0 | 2026-07-09 | Software Engineer | CP/M disk image — **create a new empty image**. **Feature:** a new **File → New Disk Image…** action builds a blank CP/M disk image of a user-chosen geometry and opens it, so files can be copied in and then written with **Save Image…** — no starting image required. It reuses the v2.32 mount-side dialog (Host or Remote pane); mounting a new image **Remote-side** gives the natural "empty virtual disk, copy host files into it locally" flow. The new image has **no source file** — it is an unnamed, valid but **non-bootable** data disk (reserved/boot tracks left `0xE5`) until the first Save Image…, which writes it and adopts that path as the source (so the pane title then names it and later saves treat it as the source). While unsaved, the pane group title and the FR-175 unsaved-changes prompt show a "(new image)" placeholder. New Disk Image… is gated by the opt-in `image_write_enabled` setting (default off) and, unlike Save Image…, is enabled whenever writing is on regardless of whether an image is already open. The empty working directory is the clean baseline, so the FR-175 guard protects files added before the first save. **The engine already existed:** a `0xE5`-filled `total_bytes` buffer is an empty CP/M disk, so the library change is a one-line factory. **Added:** FR-178 (New Disk Image… behaviour + source-less image), UIR-114 (menu item placement + writing-gated enable rule + geometry picker + "(new image)" placeholder), `utils/disk_image.create_image`. **Modified:** FR-174 (Save Image… now also targets a source-less new image — builds a fresh blank image, skips the overwrite-source check, and adopts the written path as the source); UIR-002 (File menu now lists New Disk Image…); §3.19 intro. Also corrected the Host group title to show the image affordance only for a **Host-mounted** image (a latent v2.32 issue: a Remote-mounted image could show the image name on the Host group after a resize). **Code:** `utils/disk_image/__init__.py` (`create_image` factory + `__all__`); `gui/mw_disk_image.py` (`menu_new_image`, `_prompt_new_geometry`, `_update_new_image_action`; `_repack_workdir` builds a blank image when source-less; `menu_save_image` handles no source — fallback suggested name, skipped overwrite check, adopt-on-save; `_prompt_save_discard_cancel` placeholder); `app.py` (`_new_image_action` + menu item after Open Disk Image…; `_update_host_group_title`/`_update_remote_group_title` gate on the mounted pane and show the placeholder for a source-less image); `gui/mw_config.py` (`load_config`/`menu_general_config` refresh the New Disk Image… action); `gui/mw_base.py` (typing declarations); `lang/lang_*.txt` (5 new keys — `menu.file.new_image`, `dialog.new_image.pick_title`, `dialog.new_image.pick_label`, `main.image_unsaved`, `status.new_image_created` — all 12 languages). **Tests:** `tests/test_disk_image/test_write.py` (`create_image` yields an empty, writable, round-tripping image); `tests/test_gui_smoke.py` (action enabled iff writing on; new image opens empty and source-less; "(new image)" title placeholder; Save adopts the written path as source and re-opens with the staged file; a Remote-mounted new image accepts a local copy). Full unit suite green (648 tests); `ruff`/`mypy` clean; views regenerated, 0 stale tags; FR-178/UIR-114 covered. **Integration (HIL):** no new automated case — creating/saving is host-side with no serial/protocol effect; new manual case MT-DI16 for the bench. **Docs:** user manual (§10 disk-image section — "Creating a New Disk Image" + ToC) and manual test plan/scorecard (MT-DI16, versioned 1.46→1.47) updated; `src/version.txt` / SRS version field / user-manual version line → `2.34.0`; SRS §9 row added. Making a new image bootable (copying system tracks from a system image) remains a possible later feature. |
| 2.33.1 | 2026-07-09 | Software Engineer | Bug fix (edge case in the v2.32 dual-pane mount) — opening a **Remote-side** disk image immediately after a **Host-side** one left the Host pane on a stale path. **Bug:** a Host-side mount repoints `host_dir` at the image's temporary working directory; opening a second image Remote-side then discarded that workdir (`_cleanup_image_workdir`) but the Remote-mount branch of `menu_open_image` never touched `host_dir`, so the Host pane was left pointing at the now-deleted temp folder until the user next navigated. FR-176 already requires the Host pane to keep a real directory under a Remote-side mount; this makes the code comply. **Fix:** when mounting Remote-side, if `host_dir` is no longer a valid directory (it was the previous image's discarded workdir), restore it to the directory recorded before that image was opened — falling back to the configured host directory or the current working directory — and refresh the Host pane. The common cases (Remote-side from no image, or after another Remote-side mount) are unaffected because `host_dir` is already valid. **Code:** `gui/mw_disk_image.py:menu_open_image` (Remote-mount branch restores `host_dir` when stale). **Tests:** `tests/test_gui_smoke.py::test_open_remote_after_host_restores_host_pane` (Host-side then Remote-side → the old workdir is gone and the Host pane is back on the real folder). Full unit suite green (642 tests); `ruff`/`mypy` clean; views regenerated, 0 stale tags. **Integration (HIL):** no change needed — host-side pane state with no serial/protocol effect. **Docs:** SRS FR-176 in-text note; manual test plan/scorecard (MT-DI13 clause, versioned 1.45→1.46); `src/version.txt` / SRS version field / user-manual version line → `2.33.1`. Not added to the SRS §9 table (patch-level bug fix). |
| 2.33.0 | 2026-07-09 | Software Engineer | CP/M disk image — **Close Disk Image**. **Feature:** a new **File → Close Disk Image…** action closes the currently open image without opening another, discarding its temporary working directory and open-image state and **restoring both file panes**. It is gated by the FR-175 unsaved-changes prompt: with disk-image writing enabled and staged changes, it offers **Save / Discard / Cancel** first and **Cancel** aborts the close (the image stays open). Closing a **Remote-side** mount (FR-176) resets the Remote pane — the image listing is cleared, the drive drop-down re-enabled, and the Remote group title restored (this reuses the teardown added in v2.32); closing a **Host-side** mount additionally returns the Host pane to the directory that was active **immediately before** the image was opened (falling back to the configured host directory, or the current working directory). The menu item is disabled while no image is open. **Added:** FR-177 (Close Disk Image… behaviour), UIR-113 (File-menu item placement + enable rule). **Modified:** UIR-002 (File menu now lists Close Disk Image…); FR-171 traceability (`_cleanup_image_workdir` now also fires from close). **Code:** `gui/mw_disk_image.py` (`menu_close_image`; `menu_open_image` records the pre-open host directory when mounting Host-side and enables the close action; `_cleanup_image_workdir` resets that memory and disables the close action); `app.py` (`_pre_image_host_dir`/`_close_image_action` state + the Close Disk Image… menu action after Save Image…); `gui/mw_base.py` (typing declarations); `lang/lang_*.txt` (2 new keys — `menu.file.close_image`, `status.disk_image_closed` — all 12 languages). **Tests:** `tests/test_gui_smoke.py` (action enabled only while an image is open; Host-side close restores the pre-open directory and re-lists the Host pane; Remote-side close clears the Remote listing, re-enables the drive drop-down, restores the title and leaves the Host pane untouched; a dirty close prompts and Cancel keeps the image open). Full unit suite green (641 tests); `ruff`/`mypy` clean; views regenerated, 0 stale tags; FR-177/UIR-113 covered. **Integration (HIL):** no new automated case — Close is a host-side operation with no serial/protocol effect; new manual case MT-DI15 to be confirmed opportunistically on the bench. **Docs:** user manual (§10 disk-image section — Close Disk Image + ToC) and manual test plan/scorecard (MT-DI15, versioned 1.44→1.45) updated; `src/version.txt` / SRS version field / user-manual version line → `2.33.0`; SRS §9 row added. |
| 2.32.0 | 2026-07-09 | Software Engineer | CP/M disk image **dual-pane mount** (host↔image local copy). **Feature:** opening an image now asks (a modal Host/Remote dialog, default **Host**) which pane to mount it into. **Host-side** is the established behaviour (the image is the Host pane; transfers run to/from the remote CP/M device over X-Modem). **Remote-side** mounts the image in the **Remote** pane as a *local virtual device*: the Host pane keeps its real directory, the Remote list is sourced from the image working directory (the drive drop-down is disabled and the Remote group title names the image), and **Copy to Remote** / **Copy to Host** (and drag-and-drop each way) become **local filesystem copies** into/out of the image with **no serial connection** — the into-image direction keeps CP/M 8.3 name validation (FR-148/FR-149) and destination-conflict resolution (FR-145–FR-147); the outbound direction applies conflict resolution but no 8.3 check (it produces plain host files). A Remote-side mount is **mutually exclusive** with a live serial session: it is refused while the Terminal Port is connected, and a Connect is refused while a Remote-side image is mounted (close the image first). Only one image mounts at a time; image↔image is out of scope. The copy-to-image unsaved-changes guard (FR-175) applies regardless of the mounted pane. **Added:** FR-176 (dual-pane mount + local host↔image copy + mutual exclusion), UIR-112 (mount-side dialog + Remote-pane image affordance + disabled drive drop-down). **Modified:** FR-169 (mount pane selectable), FR-171 (the working directory may back either pane), FR-073/FR-074 (Remote list sourced from the image when Remote-mounted; the not-connected handling does not apply then), FR-080/CR-010 (the connected-both-ports precondition governs X-Modem serial transfers only — the local copy is not gated), §3.19 intro. **Code:** `gui/mw_disk_image.py` (`_prompt_mount_side` dialog; `_image_pane` state; `menu_open_image` mount branch + refuse-while-connected; `_remote_is_image`; `_list_image_remote`; `_copy_host_to_image`/`_copy_image_to_host` local engine with GUI-thread `_prompt_invalid_name_local`/`_resolve_conflict_local` dialog helpers; `_cleanup_image_workdir` resets the pane, re-enables the drive drop-down, clears a stale image listing, restores the title); `gui/mw_transfer_batches.py` (`do_copy_to_remote`/`do_copy_to_host` branch to the local copy when Remote-mounted); `gui/mw_transfers.py:_on_files_dropped` (local-copy branch, no slow-transfer confirm); `gui/mw_remote.py` (`refresh_remote_files`/`change_drive`/`do_refresh_remote_files` image-sourced listing; `do_connect` refuses while Remote-mounted); `app.py` (`_image_pane` init; `remote_group` stored; `_update_remote_group_title`; retranslate cascade); `gui/mw_base.py` (typing declarations + `drive_combo`); `lang/lang_*.txt` (10 new keys — `main.remote_files_image`, `dialog.mount_side.title`/`.message`/`.host`/`.remote`, `error.image_remote_needs_disconnect`, `error.image_remote_blocks_connect`, `error.image_copy`, `status.image_copy_in`/`.image_copy_out` — all 12 languages). **Tests:** `tests/test_gui_smoke.py` (Remote-mount lists in the Remote pane + Host pane unchanged + drive drop-down disabled; Copy to Remote local-stages a Host file with 8.3 validation; Copy to Host writes out with no 8.3 check; Remote-mount refused while connected; Connect refused while Remote-mounted; Remote group title + drive drop-down + listing revert on close); the shared `_open_fake_image` helper gained a `mount` parameter and bypasses the mount-side dialog. Full unit suite green; `ruff`/`mypy` clean; views regenerated, 0 stale tags; FR-176/UIR-112 covered. **Integration (HIL):** no new automated case — the local host↔image copy is entirely host-side (no serial path) and the image↔device (Host-side) path is unchanged and already HIL-covered; new manual cases MT-DI13/MT-DI14 to be confirmed opportunistically on the bench. **Docs:** user manual (§10 disk-image section — the two mount sides + local host↔image copy) and manual test plan/scorecard (MT-DI13/MT-DI14, versioned 1.43→1.44) updated; `src/version.txt` / SRS version field / user-manual version line → `2.32.0`; SRS §9 row added. Multi-slice / CF multi-partition browsing remains for a later phase. |
| 2.31.0 | 2026-07-09 | Software Engineer | CP/M disk image **copy-to-image** (Phase 4) — download staging + an unsaved-changes save prompt. **Feature:** because opening an image points the Host pane at its temporary working directory, a file **received from the remote** (Copy to Host / drag-to-Host) while an image is open already lands in that directory and is included when the image is saved (FR-174) — so copy-to-image needs no separate transfer path (the conflict and 8.3-validation behaviours apply unchanged). Phase 4 makes that first-class and, above all, **prevents silent loss** of those staged edits: when disk-image writing is enabled **and** the working directory has unsaved changes relative to the files as opened, any action that would discard it — opening another image, **File → New**, a configuration **Load**, a host-directory change (General Config edit or **Change Directory**), or **application exit** — now first presents a modal **Save / Discard / Cancel** prompt (Cancel aborts and keeps the image open; Discard proceeds and drops the changes; Save routes through **Save Image…** and proceeds only if the save completes, so a cancelled Save-As keeps the changes). With writing off, or with no unsaved changes, the discard is silent as before. The Host Files group title now names the open image file (instead of showing the temp path) so a staged image is not mistaken for an ordinary folder. **Added:** FR-175 (copy-to-image staging + the unsaved-changes discard guard), UIR-111 (image-name group title + the Save/Discard/Cancel dialog, house button order). **Modified:** FR-171 (the working-directory discard is now gated by the FR-175 prompt when writing is enabled and there are unsaved changes; otherwise unchanged); §3.19 intro. **Code:** `gui/mw_disk_image.py` (`_workdir_signature`/`_capture_image_baseline`/`_image_is_dirty` dirty-tracking by `(name,size,mtime)` signature vs. an open/save baseline; `_maybe_prompt_save_image` guard; `_prompt_save_discard_cancel` dialog; `menu_save_image` now returns whether it saved; capture the baseline on open and after save; clear it on cleanup); `app.py` (`_image_baseline` state; `_update_host_group_title` image affordance; `closeEvent` guard that ignores the close on Cancel); the discard callers `gui/mw_disk_image.py:menu_open_image`, `gui/mw_file_panes.py:change_host_dir`, `gui/mw_config.py:load_config`/`menu_new`/`menu_general_config` call the guard first; `gui/mw_base.py` (typing declarations); `lang/lang_*.txt` (4 new keys — `main.host_files_image`, `button.discard`, `dialog.image_dirty.title`, `dialog.image_dirty.message` — all 12 languages). **Tests:** `tests/test_gui_smoke.py` (a received file staged in the working dir is written on save; the guard prompts only when open + writing on + dirty and maps Save/Discard/Cancel to proceed-if-saved / proceed / abort; New aborts on Cancel; Change Directory routes through Save on Save; closeEvent Cancel ignores the close and keeps the image open; the group title names the open image). Full unit suite green; `ruff`/`mypy` clean; views regenerated, 0 stale tags; FR-175/UIR-111 covered. **Integration (HIL):** no new HIL case needed — the received-file path is the already-HIL-covered Copy-to-Host/X-Modem path and the new logic is host-side GUI state (dirty prompt, group title) with no protocol effect; to be confirmed opportunistically on the bench (MT-DI11/MT-DI12). **Docs:** user manual (§10 disk-image section — copy-to-image + unsaved-changes prompt — and ToC) and manual test plan/scorecard (MT-DI11/MT-DI12, versioned 1.42→1.43) updated; `src/version.txt` / SRS version field / user-manual version line → `2.31.0`; SRS §9 row added. Multi-slice / CF multi-partition browsing (Phase 5) remains for later. |
| 2.30.0 | 2026-07-09 | Software Engineer | CP/M disk image **write** support (Phase 3), opt-in. **Feature:** a new **File → Save Image…** action re-packs the current Host working directory (the files extracted from an open image, as edited by the user — files added, deleted, or replaced) into a **fresh CP/M disk image** written, via a Save-As dialog, to a **user-chosen new file**. The source image is **never** modified in place (choosing the source path is refused with a warning), and the opened image's **reserved/boot tracks and geometry are preserved verbatim** so the written image remains bootable. Writing is gated behind an opt-in **`image_write_enabled`** setting (a checkbox on the General Config dialog, **default off**); while off the disk-image feature stays read-only and Save Image… is disabled. The re-pack allocates data blocks sequentially after the directory, splits each file into the correct extents/allocation map (8- or 16-bit pointers), sets the `EX`/`S2`/`RC` counters so a re-open reads every file back byte-identical, and aborts with an explanatory error — writing nothing — when the working directory holds more files than the directory allows (`maxdir`), more data than the data area holds (`dsm`), or an invalid CP/M 8.3 name. **Added:** FR-174 (Save Image… behaviour + opt-in gate), UIR-110 (File-menu item placement + General-Config checkbox + enable/disable rules), DR-050 (the write / re-pack format — the inverse of DR-049: sequential allocation, extent/`AL`/`EX`/`S2`/`RC` encoding, skew-aware sector writing, boot-track preservation, typed capacity/name errors). **Modified:** UIR-002 (File menu now lists Save Image…); §3.19 intro (write is now supported, opt-in). **Code:** `utils/disk_image/filesystem.py` (new `ImageWriteError`/`DiskFullError`/`DirectoryFullError`/`InvalidNameError`, `split_83`, `build_dir_entries`, `_encode_al`); `utils/disk_image/image.py` (`_raw` now mutable; `_write_data`/`write_block` skew-aware writer; real `write_file`/`delete_file`/`save` replacing the `NotImplementedError` stubs, plus `_first_data_block`/`_used_blocks`/`_free_dir_slot_offsets`/`_erase` helpers); `gui/mw_disk_image.py` (`menu_save_image`, `_repack_workdir`, `_image_write_enabled`, `_update_save_image_action`; capture `_image_geom` at open; clear + disable on cleanup); `app.py` (Save Image… menu action + `_image_geom`/`_save_image_action` state); `gui/mw_config.py:menu_general_config` (refresh the action after a settings change); `gui/config_dialogs.py:GeneralConfigDialog` (new `image_write_enabled` checkbox); `utils/config_handler.py` (`image_write_enabled` default `OFF`); `gui/mw_base.py` (typing declarations); `lang/lang_*.txt` (7 new keys — `menu.file.save_image`, `dialog.save_image.title`/`.filter`, `status.disk_image_saved`, `error.disk_image_write`, `error.disk_image_overwrite_source`, `config.general.image_write` — all 12 languages). **Tests:** new `tests/test_disk_image/test_write.py` (write→save→re-open round-trips: single/multi-extent, 16-bit pointers, empty/odd-length, attributes; matches the independent synthetic builder; boot-track preservation; delete-frees-blocks-for-reuse; DiskFull/DirectoryFull/InvalidName errors; `split_83`); `tests/test_gui_smoke.py` (Save Image… gated by setting + image state, no-op when disabled, end-to-end re-pack to a new file, source-overwrite refused). Full unit suite green; `ruff`/`mypy` clean; views regenerated, 0 stale tags; FR-174/DR-050/UIR-110 covered. **Integration (HIL):** no new HIL case needed — Save Image… is a host-side filesystem operation with no serial/protocol interaction; the *upload* of a written/extracted file still traverses the already-HIL-covered Copy-to-Remote/X-Modem path. To be confirmed opportunistically on the bench (write an image, mount it on real hardware). **Docs:** user manual (§10 "Saving a CP/M Disk Image" + ToC + Default Settings) and manual test plan/scorecard (MT-DI09/MT-DI10, versioned 1.41→1.42) updated; `src/version.txt` / SRS version field / user-manual version line → `2.30.0`; SRS §9 row added. Copy-to-image (download into a mounted image, Phase 4) and multi-slice CF browsing (Phase 5) remain for later. |
| 2.29.1 | 2026-07-09 | Software Engineer | Bug fix — the app leaked temporary working directories onto the host (6000+ orphaned `cpm_fm*` folders reported). **Bug:** the remote **View** download (FR-113a) created a fresh `tempfile.mkdtemp(prefix="cpm_fm_")` folder for every viewed file and **never removed it** — each view leaked a folder permanently. The disk-image workdir (FR-171, `cpm_fm_img_*`) was cleaned on the normal path but orphaned whenever the app crashed or was killed before `closeEvent`. Nothing ever swept **historical** folders from prior sessions, so they accumulated without bound. **Fix:** a new GUI-free helper `utils/temp_cleanup.py` centralises temp-dir creation under one shared `cpm_fm_*` prefix (`make_temp_dir`, with the disk-image workdir keeping its `img_` tag) and provides `sweep_temp_dirs`, which removes every `cpm_fm_*` directory in the OS temp dir — this session's remote-View downloads and image workdir **plus any orphaned by a previous session**. The sweep runs both on exit (`app.py:closeEvent`) and at start-up (`app.py:main`, so a crash-orphaned set is reclaimed on the next launch). A directory still held open (external viewer / another instance) is skipped via `rmtree(ignore_errors=True)` rather than aborting the sweep, and reclaimed by a later sweep. Stray prefix-matching *files* are left untouched. **Modified:** FR-016 (Exit now removes all session temp dirs and sweeps the OS temp dir on exit and start-up for orphaned `cpm_fm_*` dirs), FR-113a (remote-View temp folder is swept on exit/start-up). **Code:** new `utils/temp_cleanup.py` (`TEMP_PREFIX`, `make_temp_dir`, `sweep_temp_dirs`); `gui/mw_context_menu.py:_download_and_view` and `gui/mw_disk_image.py:menu_open_image` route through `make_temp_dir`; `app.py:main` (start-up sweep) and `app.py:closeEvent` (exit sweep). **Tests:** new `tests/test_temp_cleanup.py` (shared-prefix creation, `img_` tag, sweep removes current + historical dirs, leaves unrelated dirs and stray prefix files, tolerates an unremovable dir). Full unit suite green; `ruff`/`mypy` clean; views regenerated, 0 stale tags; FR-016/FR-113a/FR-171 now have a verifying test. **Integration (HIL):** no change needed — temp-dir cleanup is host-side filesystem behaviour with no protocol or GUI-over-serial effect. **Docs:** user manual note (temporary-file cleanup) and manual test plan/scorecard (new MT-V12, versioned 1.40→1.41) updated; `src/version.txt` / SRS version field / user-manual version line → `2.29.1`. Not added to the SRS §9 table (patch-level bug fix). |
| 2.29.0 | 2026-07-09 | Software Engineer | CP/M disk image — details view + detection hardening (Phase 2, partial). **Feature:** (1) a new **File → Image Details…** action opens a modal, read-only table of the currently open image's files with their CP/M metadata — **Name**, **Size** (bytes), **User** number, and the read-only/system/archive **Attributes** (R/S/A) — captured at open time (the extracted plain host files no longer carry it). The action is disabled while no image is open. (2) **Detection hardening:** directory-validity scoring now rejects any in-use entry whose allocation-map block pointers reference a block beyond the volume's data-block count (`dsm`), so a same-size foreign or corrupt file whose directory region holds impossible pointers is no longer accepted as that geometry; and when two candidates remain tied within the ambiguity margin the more capacious geometry (larger `maxdir`) ranks first, so the auto-selected default never silently truncates the directory (the 8 MB RomWBW hd1k / RC2014 pair — identical size, reserved area and data-block count, differing only in directory capacity — now defaults to hd1k while still prompting). (3) **Broadened bundled `diskdefs`:** added the RomWBW ROM-disk formats (rom128/256/384/896), a generic 4 MB hard disk, and several classic-machine floppy layouts (Amstrad PCW, Epson QX-10, Royal Alphatronic, Interak), all single-slice/standard-skew and taken verbatim from the RomWBW/cpmtools sources (multi-slice `offset` defs and libdsk/`logicalextents` formats remain out of scope). **Added:** FR-173 (Image Details view), UIR-109 (File-menu item + read-only details dialog). **Fix (folded in):** an open disk image was not discarded when the Host pane moved off it via **Load** (a different configuration), a General Config host-directory edit, or **Change Directory** — the pane repainted to the new directory but the temp working dir leaked and the stale image stayed viewable (Image Details… remained enabled). Now all three paths call the same cleanup as File → New (`mw_config.py:load_config`/`menu_general_config`, `mw_file_panes.py:change_host_dir`); `load_config` falls back to the working directory when the config names no host directory and an image was open. **Modified:** FR-170 (allocation-pointer range check + capacity tie-break), FR-171 (discard the image on config load / host-directory change too), DR-048 (broadened coverage), UIR-002 (File menu now lists Image Details…). **Code:** new `gui/disk_image_details_dialog.py:DiskImageDetailsDialog`; `gui/mw_disk_image.py` (`menu_image_details`, capture `_image_files` at open, clear + disable action on cleanup); `app.py` (menu item + `_image_details_action`/`_image_files` state); `gui/mw_base.py` (typing declarations); `utils/disk_image/__init__.py` (`_score_directory` allocation-pointer check, `detect_diskdef` capacity tie-break); `utils/disk_image/data/diskdefs` (new stanzas); `lang/lang_*.txt` (7 new keys, all 12 languages). **Tests:** `tests/test_disk_image/test_detect.py` (allocation-pointer rejection, 8 MB capacity-default), `test_diskdefs.py` (new bundled names/sizes); `tests/test_gui_smoke.py` (details dialog contents, action enable/disable, no-op when none open). Full unit suite green; `ruff`/`mypy` clean; views regenerated, 0 stale tags; FR-170/FR-173/UIR-109/DR-048 covered. **Integration (HIL):** no new case needed — the read-only details view is a local GUI-only feature and the upload path is unchanged (still the already-HIL-covered Copy-to-Remote/X-Modem path); no protocol/over-serial behaviour changed. **Docs:** user manual (§10 "Viewing CP/M File Details" + ToC) and manual test plan/scorecard (MT-DI07/MT-DI08, versioned 1.39→1.40) updated; `src/version.txt` / SRS version field / user-manual version line → `2.29.0`; SRS §9 row added. The cpmtools oracle test harness (deferred by request; cpmtools is not on the dev PATH) and write/multi-slice support remain for later phases. |
| 2.28.0 | 2026-07-08 | Software Engineer | CP/M disk image read support (Phase 1). **Feature:** a new **File → Open Disk Image…** action opens a CP/M raw-sector disk image (`.img`/`.dsk`/`.cpm` and RC2014 CompactFlash images) on the host, lists the files it contains in the Host pane, and makes them available to the existing Copy-to-Remote / drag-and-drop / X-Modem path — read-only in this phase (no write-back). Because a CP/M image carries no geometry, the layout is **auto-detected** from a bundled cpmtools-format `diskdefs` database (common floppies, the **RomWBW** fd144/fd120/fd720/fd360, hd1k and hd512 formats — taken verbatim from RomWBW v3.6 `Source/Images/diskdefs` — and the RC2014 slice) by size-matching and directory-validity scoring, prompting the user to choose only when detection is ambiguous or fails (e.g. the 8 MB RomWBW hd1k and RC2014 slice share a size). The `diskdefs` parser resolves cpmtools `offset` unit suffixes (`K`/`M`/`T`/`S`) so RomWBW multi-slice definitions load. The image's files are extracted to a temporary working directory that becomes the Host directory, so the entire established transfer/conflict/validation path applies unchanged and `xmodem.py`/the transfer mixins are untouched. The temp directory is discarded on File → New, on opening another image, and on exit. **Added:** FR-169 (open + list), FR-170 (auto-detect geometry + manual override), FR-171 (extract-to-temp + reuse existing transfer path + cleanup), FR-172 (graceful rejection of bad/foreign images), UIR-108 (File-menu item + geometry-picker dialog), DR-048 (diskdefs geometry database), DR-049 (32-byte directory-entry format + extent/AL/RC reconstruction). **Modified:** UIR-002 (File menu now lists Open Disk Image…). **Code:** new pure-Python `utils/disk_image/` package (`geometry.py`, `diskdefs.py`, `directory.py`, `image.py`, `filesystem.py`, `__init__.py` public API `load_diskdefs`/`detect_diskdef`/`open_image`, bundled `data/diskdefs`) — no GUI/third-party imports (CR-014); new `gui/mw_disk_image.py:_DiskImageMixin` wired into `app.py` (mixin list, menu item, state init, close cleanup); `gui/mw_config.py:menu_new` and `gui/mw_base.py` updated for cleanup + cross-mixin typing; `lang/lang_*.txt` (10 new keys, all 12 languages). **Tests:** new `tests/test_disk_image/` (diskdefs parsing, skew/geometry maths, directory reconstruction incl. multi-extent + tail truncation + attributes, auto-detect ranking/ambiguity, `open_image` robustness) with a synthetic-image builder; `tests/test_gui_smoke.py` (open→extract→list, foreign-file rejection, close cleanup). Full unit suite green (591 tests); `ruff`/`mypy` clean; views regenerated, 0 stale tags; FR-169–172/DR-048/DR-049/UIR-108 covered. **Integration (HIL):** no new case needed — files extracted from an image upload through the **already-HIL-covered** Copy-to-Remote/X-Modem path; to be confirmed opportunistically on the bench. **Docs:** user manual (§10 "Opening a CP/M Disk Image" + ToC) and manual test plan/scorecard (new §9.2, MT-DI01–MT-DI06, versioned 1.38→1.39) updated; `src/version.txt` / SRS version field / user-manual version line → `2.28.0`; SRS §9 row added. cpmtools oracle test harness and multi-slice CF browsing are deferred to Phase 2+. |
| 2.27.1 | 2026-07-08 | Software Engineer | UI wording — the Terminal Config **Macros** tab's inner slot tabs are relabelled **"Button `<n>`" → "Macro `<n>`"** ("Macro 1" … "Macro 10"). Cosmetic label change only; no behaviour, settings, or macro-slot semantics change. **Modified:** UIR-103d (inner-tab label wording). **Code:** `gui/config_dialogs.py:TerminalConfigDialog.create_widgets` (label lookup `config.macros.button` → `config.macros.macro`; docstring/comments); `lang/lang_*.txt` (i18n key `config.macros.button` renamed to `config.macros.macro`, value → "Macro {n}", translated in all 12 languages). **Tests:** `tests/test_macros.py` (inner-tab labels now `Macro 1..10`). Full unit suite green; `ruff`/`mypy` clean; views regenerated, 0 stale tags. **Integration (HIL):** no change needed — no HIL/visual assertion checks the macro-tab labels, and the change has no protocol or GUI-over-serial effect. **Docs:** user manual (Macros tab) and manual test plan/scorecard (MT-G12/MT-G13, versioned 1.37→1.38) updated; `src/version.txt` / SRS version field / user-manual version line → `2.27.1`. Not added to the SRS §9 table (patch-level wording fix). |
| 2.27.0 | 2026-07-08 | Software Engineer | Configurable whole-drive erase sequence for Restore. **Feature:** the Restore destination-wipe (FR-153) previously deleted the remote drive one file at a time (`delete_remote_cmd`, `ERA $1`, per file) — correct and portable, but slow on a full directory because each `ERA` pays the terminal-capture settle. A user can now supply an optional **Erase All** macro sequence that clears the whole drive in a single operation. When configured it runs **once** instead of the per-file loop; a typical value answers CP/M's interactive `ALL (Y/N)?` prompt (e.g. `SEND ERA *.*` / `WAIT 1` / `SEND Y`, or `WAITFOR (Y/N)` for a timing-robust variant). The macro uses the existing boot-sequence/macro directive language (`SEND`/`SENDRAW`/`WAIT`/`WAITFOR`), reusing the `parse_boot_sequence`/`_execute_sequence` engine. **Empty (the default) keeps the per-file behaviour unchanged**, and a macro that fails to parse falls back to the per-file loop rather than leaving the drive un-erased — so all existing configurations are unaffected. Backup (host wipe) is untouched. **Added:** FR-153e (the optional erase-all sequence + fallback), UIR-107 (the multi-line "Erase All" field in the General Config dialog's Remote group; new `erase_all_remote_seq` setting, default empty). **Modified:** FR-153d (per-file wipe now applies unless the FR-153e sequence is configured). **Code:** `gui/mw_backup_restore.py:_wipe_remote_drive` (parse + run the sequence once when set, else the per-file loop; `Satisfies: FR-153e`); `gui/config_dialogs.py:GeneralConfigDialog` (new `multiline` `erase_all_remote_seq` field, last in the Remote group); `utils/config_handler.py` (`erase_all_remote_seq` default `""`); `gui/mw_base.py` (`_execute_sequence` stub for the cross-mixin call); `lang/lang_*.txt` (new `config.general.erase_all` label, all 12 languages). **Tests:** `tests/test_backup_restore.py` (macro runs once and skips the per-file loop; whitespace-only macro falls back to per-file; malformed macro falls back to per-file); `tests/test_gui_smoke.py::test_general_config_remote_group_first` extended for the new field (tagged `Verifies: UIR-107`). Full unit suite green; `ruff`/`mypy` clean; views regenerated, 0 stale tags; FR-153e/UIR-107 covered. **Integration (HIL):** `integration/test_gui_backup_restore.py` extended with an erase-all-sequence Restore case (MT-BR10); it needs a real CP/M peer and the `--run-destructive` flag, so its **bench run is pending** (no hardware at hand). **Docs:** user manual (Config → General Remote-command table + Reference: Default Settings) and manual test plan/scorecard updated and versioned; `src/version.txt` / SRS version field / user-manual version line → `2.27.0`. |
| 2.26.4 | 2026-07-07 | Software Engineer | Bug fix — after an X-Modem batch, the auto-refresh omitted the just-transferred file(s) on a slow CP/M peer. **Bug (found on a new HIL target, the RC2014 Mini):** on completing a Copy to Remote batch the app fired the FR-099 Remote-list refresh (`DIR`) immediately, with no settle. Between files FR-109 already waits the `xfer_interfile_delay` for the CCP prompt to return, but **not after the final file** — so on a machine slower to return from `XMODEM.COM` the refresh `DIR` was captured before the prompt had come back and the just-uploaded file was missing from the list (it only appeared on a later manual Update). Confirmed a real GUI defect, not a test artifact: the file was written correctly and showed up in the next listing. **Fix:** apply the `xfer_interfile_delay` settle after the final file of a batch, before emitting `transfer_completed` (the FR-099 refresh) — reusing the existing setting, no new configuration. Applied to both `_transfer_to_remote_batch` and `_transfer_to_host_batch` (guarded on ≥1 file transferred), cancellable so a Cancel is still observed promptly (FR-120). **Modified:** FR-109 (settle also applied after the final file, before the post-batch refresh). **Code:** `gui/mw_transfer_batches.py` (`_transfer_to_remote_batch`/`_transfer_to_host_batch`: `_cancellable_sleep(_interfile_delay())` before the completion emit). **Tests:** `tests/test_gui_smoke.py` (`test_remote_batch_settles_after_final_file_before_refresh`, `test_host_batch_settles_after_final_file_before_refresh` — the settle waits the inter-file delay before the refresh; `test_batch_does_not_settle_when_nothing_transferred` — no settle when the batch skipped every file). Full unit suite green; `ruff`/`mypy` clean; views regenerated, 0 stale tags. **Integration (HIL):** mirrored the settle in the harness peer (`integration/helpers/peer.py:send_file`/`recv_file` wait `xfer_interfile_delay` after the transfer, after resuming the shared-port read loop) so the existing upload-then-list cases reflect the app; also **added the RC2014 Mini target** to `integration/hil_config.json`. **Bench run PASSED on the RC2014 Mini**: the six previously failing cases (protocol round-trip, GUI conflict/overwrite/apply-to-all, filename validation, remote rename, backup download) all pass — the just-transferred file now appears in the post-batch refresh. **Docs:** manual test plan/scorecard (MT-T08 extended, versioned 1.35→1.36); user manual (Xfer Inter-file Delay reference) updated; `src/version.txt` / SRS version field / user-manual version line → `2.26.4`. |
| 2.26.3 | 2026-07-07 | Software Engineer | Bug fix (follow-up to 2.26.1) — Disconnect still hung ~30 s on a real two-port RTS/CTS rig (eZT-RCB) with the ports configured back-to-front; the HIL case MT-C18 caught it. **Root cause:** the 2.26.1 bounded `write_timeout` bounds pyserial `WriteFile` calls but **not** the OS close-time flush. On Windows, `CloseHandle` on a serial port waits for the driver's queued transmit bytes to drain; under RTS/CTS flow control with a miswired link the peer never asserts CTS, so the connect probe's queued bytes can never leave and `close()` blocked for tens of seconds. **Fix:** discard the port's queued transmit/receive buffers immediately **before** closing — `reset_output_buffer()` issues `PurgeComm(PURGE_TXABORT | PURGE_TXCLEAR)`, aborting the pending write and clearing the queue so `close()` returns promptly (mirrors the X-Modem cancel abort's flow-control-safe drain, v2.13.1). Best-effort: ports/stubs without the reset methods are ignored. **Modified:** FR-050 (also purge the queued buffers before closing). **Code:** new `terminal/serial_manager.py:_purge_before_close`, called from `close_terminal_port`, `close_transport_port`, and `close_ports` before each `close()`. **Tests:** `tests/test_serial_manager.py` (purge precedes close for both ports; close still succeeds when the port lacks reset methods; `_FakePort` records a reset/close event order). Full unit suite green; `ruff`/`mypy` clean; views regenerated, 0 stale tags. **Integration (HIL):** no new case — the existing `test_disconnect_prompt_with_ports_swapped` (MT-C18) is the regression guard. **Bench run PASSED on both two-port RTS/CTS rigs (eZT-RCB and MinZ)**: the swapped-port disconnect completed in **0.03–0.04 s** (was ~30 s), and the full `test_gui_connect.py` set (7 tests) passed on each. **Docs:** `src/version.txt` / SRS version field / user-manual version line → `2.26.3`. |
| 2.26.2 | 2026-07-07 | Software Engineer | Bug fix — loading a new configuration while connected left the old ports open. **Bug:** with a configuration loaded and the COM ports open, **File → Load** of a different configuration did not close the original ports first — unlike **File → New** (FR-019), which already does. `load_config` replaced `self.settings` outright, so `SerialManager` kept the prior config's ports open with the Terminal/Transport status flags still set, while the settings now described different ports. The app reported *connected* on ports that no longer matched the loaded configuration. **Fix:** `load_config` now performs the Disconnect close (FR-050–FR-058) **before** replacing the settings, guarded on the connected flags so nothing happens when no port is open (the start-up reload of the last-used file, FR-005, is unaffected). The close runs while `self.settings` still names the open ports — `do_disconnect` derives the port names from the current settings, so the ordering matters. A close failure does not abort the load (matching File → New). **Added:** FR-017a (close open ports before loading a configuration). **Code:** `gui/mw_config.py:load_config` (guarded `do_disconnect()` before the settings swap; `Satisfies: FR-017a`). **Tests:** `tests/test_gui_smoke.py` (`test_load_config_disconnects_open_ports_before_swap` — disconnect runs before the swap and sees the prior config's port; `test_load_config_skips_disconnect_when_not_connected` — no disconnect when nothing is open). Full unit suite green; `ruff`/`mypy` clean; views regenerated, 0 stale tags; FR-017a covered. **Integration (HIL):** new `integration/test_gui_connect.py::test_load_config_while_connected_closes_ports` (MT-C19) connects, reloads a configuration, and asserts the ports are closed and the flags cleared; **bench run PASSED on the eZT-RCB and MinZ two-port rigs**. **Docs:** user manual (File menu — Load) and manual test plan/scorecard updated and versioned; `src/version.txt` → `2.26.2`. |
| 2.26.1 | 2026-07-07 | Software Engineer | Bug fix — Disconnect hung for tens of seconds when the Terminal and Transport COM ports were configured back-to-front. **Bug (two reports, one root cause):** with the ports swapped the post-connect probe cannot reach the remote, and Disconnect stalled for tens of seconds — both when pressing **Disconnect** directly (probe still running) and when clicking **Abort** on the Remote Filesystem Unavailable dialog (both go through `do_disconnect`). Two contributing faults: (1) the ports were opened with **no write timeout**, so a serial write — and the pyserial `close()` that waits for pending output to drain — could block indefinitely when the miswired link could not transmit; (2) the connect probe (FR-041–FR-048) was **not cancellable and not stopped on Disconnect**, so its serial I/O contended with the port close. **Fix:** (1) both ports are now opened with a bounded **write timeout** (default 2000 ms, new `terminal_write_timeout_ms`/`transport_write_timeout_ms` settings; a safety bound, not surfaced in the config dialog); (2) `do_disconnect` now sets a **probe-cancel** event and joins the probe worker (bounded) before closing, and the probe's capture waits poll that event so they wake promptly and the probe returns without emitting when cancelled. **Modified:** FR-030, FR-038 (ports opened with a bounded write timeout), FR-050 (Disconnect cancels the in-flight probe before closing so it stays prompt and the UI responsive). **Code:** `terminal/serial_manager.py:open_port` (`write_timeout` param, per-port config + 2s fallback); `utils/config_handler.py` (new write-timeout defaults); `app.py` (`_probe_cancel` event, `_probe_thread` handle); `gui/mw_base.py` (declarations); `gui/mw_transfers.py:_cancellable_sleep` (optional `cancel_event`, defaults to the transfer flag); `gui/mw_remote.py` (`do_connect` clears/tracks the probe worker; `_do_connect_probe_logic`/`_probe_for_drive` bail on cancel; `_capture_terminal_response` accepts a `cancel_event`; `do_disconnect` cancels + joins the probe before closing). **Tests:** `tests/test_serial_manager.py` (write-timeout default, per-port, non-numeric fallback); `tests/test_gui_smoke.py` (Disconnect cancels + joins an in-flight probe before closing; probe stops without emitting when cancelled; capture bails early on `_probe_cancel`); existing probe-stub lambdas updated for the new `cancel_event` kwarg. Full unit suite green; `ruff`/`mypy` clean; views regenerated, 0 stale tags. **Integration (HIL):** new `integration/test_gui_connect.py::test_disconnect_prompt_with_ports_swapped` (MT-C18, two-port only) configures the ports back-to-front and asserts Disconnect completes well within a bound; **bench run PASSED on the eZT-RCB and MinZ two-port rigs** (see 2.26.3, which completed the fix — the write timeout alone was insufficient). **Docs:** user manual (Troubleshooting + Reference: Default Settings) and manual test plan/scorecard updated and versioned; `src/version.txt` → `2.26.1`. |
| 2.26.0 | 2026-07-07 | Software Engineer | Terminal Window terminal-type status bar. **Feature:** the Terminal Window's "Type here — keystrokes are sent live" input-hint label (below the Receive view) is removed and replaced by a **status bar** that shows the terminal emulation currently in use (e.g. `Emulation: VT100`). The status bar reflects the active type when the window opens and updates immediately when the type changes — via the Receive-view context-menu **Terminal Type** submenu (UIR-101) or a **Config → Terminal** save (UIR-034). The status text follows the active UI language (FR-123). **Added:** UIR-106 (Terminal Window terminal-type status bar). **Modified:** UIR-064 (the input-hint label below the Receive view is now a status bar), UIR-067 (the input-hint label is removed). **Code:** `gui/terminal_window.py` (removed the `lbl_hint` QLabel; added `update_terminal_type_status`, called from `create_widgets`, `render_screen`, and `retranslate_ui`); `gui/mw_config.py` (`_apply_terminal_settings` refreshes the open window's status bar); `lang/*.txt` (removed `terminal.input_hint`, added `terminal.status_type = Emulation: {type}`, all 12 languages). **Tests:** `tests/test_gui_smoke.py` (status bar shows/updates the active type, follows the language, and refreshes on a Terminal Config apply). Full unit suite green; `ruff`/`mypy` clean; views regenerated, 0 stale tags; UIR-106 covered (UIR-067 remains verification method **I**). **Integration (HIL):** the terminal-window visual assertion (MT-W13) extended to confirm the status bar shows the active terminal type (UIR-106). Bench run **pending** (no hardware at hand). **Docs:** user manual (Terminal Window layout) and manual test plan/scorecard updated and versioned; `src/version.txt` → `2.26.0`. |
| 2.25.0 | 2026-07-07 | Software Engineer | Terminal Config dialog, Terminal Window cleanup, and session window restore. **Feature:** (1) a new **Config → Terminal** dialog (tabbed) gathers the Terminal settings — the **Terminal Type** dropdown (moved out of Config → Serial), a **Local Echo** checkbox, and an **Autoscroll** checkbox — on a *Terminal* tab, and folds the former Macro Buttons dialog in as a *Macros* tab (ten Button 1..10 slots, each Label/Keystrokes/Test); the standalone **Config → Macro Buttons** menu item is gone. (2) The Terminal Window's bottom **control row is removed entirely** — the Clear, Boot into CP/M, Macros, Local Echo, Autoscroll and Font controls no longer appear below the Receive view. Clear/Font remain on the right-click context menu; **Boot into CP/M** becomes a context-menu action (enabled only when a boot sequence is configured); Local Echo/Autoscroll move to the Terminal Config; the floating **Macro Window is removed** (macros run from the context-menu Macros submenu). (3) The **Transfer History** window is now **non-modal**. (4) On exit the app remembers which of the **Terminal Window** and **Transfer History** windows were open and **reopens them on start-up**. **Added:** UIR-103/103a–103d (Terminal Config dialog + tabs/fields), UIR-104 (`autoscroll` setting), UIR-105 (context-menu Boot action), FR-021c (Config → Terminal save), FR-168 (session window restore); new settings `local_echo` (default OFF) and `autoscroll` (default ON). **Modified:** UIR-003 (Config menu items), UIR-034 (Terminal Type now on the Terminal dialog), UIR-062/UIR-098/UIR-102 (autoscroll/macros wording), UIR-064/067/069/099 (terminal-window layout + context menu), UIR-083/FR-143 (History non-modal), FR-004 (geometry list), FR-021b (macros are the Terminal dialog's Macros tab), FR-049 (Boot via context menu), FR-093 (Local Echo is a setting), FR-162 (macros via context menu). **Removed:** UIR-065, UIR-066, UIR-068, UIR-096, UIR-097, FR-164 (the control-row checkboxes/button and the Macro Window). **Code:** deleted `gui/macro_window.py` and `gui/flow_layout.py`; `gui/config_dialogs.py` (`SerialConfigDialog` loses Terminal Type; `MacroConfigDialog` → `TerminalConfigDialog` with a Terminal tab + Macros tab); `gui/terminal_window.py` (control row removed; context-menu Boot via `boot_enabled_provider`; `set_autoscroll`); `gui/mw_config.py` (`menu_terminal_config`, `_apply_terminal_settings`); `gui/mw_remote.py` (macro-window methods removed; `_configured_macros` kept); `gui/mw_history.py`/`gui/transfer_history_dialog.py` (non-modal, reuse, retransfer via `finished`); `gui/window_state.py` (`window_open`/`set_window_open`); `app.py` (Config → Terminal menu item, `_restore_open_windows`, close-time open-window recording, `_history_dialog` state); `utils/config_handler.py` (`local_echo`/`autoscroll` defaults); `gui/mw_base.py`; `lang/*.txt` (new Terminal-config + `terminal.menu.boot` keys, dead keys removed, all 12 languages). **Tests:** `tests/test_macros.py` rewritten (TerminalConfigDialog Terminal/Macros tabs, round-trips, Test button, Config → Terminal save); `tests/test_gui_smoke.py` (six-item context menu incl. Boot, boot-enabled provider, terminal-settings application, non-modal history reuse + retransfer-on-finished, window restoration, close-time recording); `tests/test_window_state.py` (window-open round-trip/persistence). Full unit suite green (542 tests); `ruff`/`mypy` clean; views regenerated, 0 stale tags; all new testable IDs covered. **Integration (HIL):** obsolete Macro-Window case (MT-W14) removed — macros over serial are already covered by the context-menu Macros submenu case; the terminal-window visual assertion rewritten to confirm no control row and Font in the context menu (UIR-064/069). Bench run **pending** (no hardware at hand). **Docs:** user manual (Terminal Window, Config → Terminal, Config → Serial, Reference: Default Settings) and manual test plan/scorecard updated and versioned; `src/version.txt` → `2.25.0`. |
| 2.24.0 | 2026-07-07 | Software Engineer | Terminal Window context-menu **submenus**. **Feature:** the Receive-view right-click menu (UIR-099) gains two submenus after Reset Size — a **Terminal Type** submenu and a **Macros** submenu. **Terminal Type** lists VT100/VT52/ADM-3A as checkable items with the active type checked; choosing one switches the running terminal's emulation immediately (the same live effect as the Config → Serial dropdown, persisted with the config on the next save). **Macros** lists the configured macro buttons by label and runs the chosen macro's keystroke script on the Terminal Port; it is disabled when no macros are configured. **Approach:** the Terminal Window builds both submenus in `_build_context_menu` from new owner-supplied callbacks/providers (current type read from the engine; the macro list factored into a shared `_configured_macros` helper reused by the Macro Window). **Added:** UIR-101 (Terminal Type submenu), UIR-102 (Macros submenu). **Modified:** UIR-099 (notes the two submenus), UIR-034 (menu selection added as an apply point). Menu-title labels added to all 12 language files. |
| 2.23.0 | 2026-07-07 | Software Engineer | Terminal Window right-click **context menu**. **Feature:** right-clicking the Receive view opens a context menu with five items — **Copy**, **Paste**, **Clear**, **Font…**, and **Reset Size (24×80)**. Copy places the highlighted text on the system clipboard; Paste transmits the clipboard's text on the Terminal Port as typed input (line separators normalised to the configured EOL); Clear and Font… reuse the existing clear (FR-095) and font-dialog (UIR-069) behaviour; Reset Size reflows the grid to 80×24. **Approach:** the custom-painted `TerminalView` grid had no mouse handling, so left-button click-drag **text selection** (highlighted via the palette highlight colours) was added to back Copy; the menu itself is built by the Terminal Window and shown from a right-click hook the view reports. Reset Size resizes the window by the viewport delta and lets the existing reflow (FR-091a) settle the geometry. **Added:** UIR-099 (the five-item context menu, Copy disabled with no selection), UIR-100 (mouse text selection), FR-165 (Copy → clipboard, trailing blanks trimmed per row, rows joined by newline), FR-166 (Paste → Terminal Port, EOL-normalised, buffered/echoed, not-connected guarded), FR-167 (Reset Size → 80×24). Menu labels added to all 12 language files. |
| 2.22.0 | 2026-07-06 | Software Engineer | Selectable terminal emulation (VT-52 / ADM-3A) as a serial-config option. **Feature:** the Terminal Window can now be configured to emulate a **VT-52** or a Lear Siegler **ADM-3A** in addition to the default **VT-100/ANSI**, via a new **Terminal Type** dropdown in Config → Serial. The selected type governs both how the remote's output is interpreted/rendered and how the cursor keys typed into the terminal are encoded. **Approach:** rather than a second renderer, each legacy type is handled by a stateful byte-stream *translator* that rewrites its control/escape sequences into the equivalent VT-100/ANSI before they reach the existing `pyte`-backed screen model — so the one renderer, scrollback, and reflow are shared. **Added:** FR-157i (VT-52 emulation: `ESC A`/`B`/`C`/`D` cursor moves, `ESC H` home, `ESC I` reverse-LF, `ESC J`/`K` erase, `ESC Y row col` direct addressing, `ESC F`/`G` graphics→DEC line-drawing, keypad/identify/ANSI-mode sequences consumed), FR-157j (ADM-3A emulation: Ctrl-K/Ctrl-L/RS cursor up-right-home, Ctrl-Z clear-and-home, BS/LF/CR pass-through, `ESC = row col` direct addressing; no attributes/colour), FR-158a (VT-52 arrow keys → `ESC A`/`B`/`C`/`D`, no `[`), FR-158b (ADM-3A arrows → Ctrl-K/Ctrl-J/Ctrl-H/Ctrl-L, Home → RS), UIR-034 (the Terminal Type dropdown, `terminal_type` setting, options VT100/VT52/ADM-3A, default VT100, applied to the engine on config load and on serial-config save). **Modified:** FR-157 (now selects among the emulation types), FR-158 (its VT-100 key mapping is the default; VT-52/ADM-3A override the cursor keys per FR-158a/b). Both direct-address forms are 0x20-biased and 0-based; malformed coordinates clamp rather than raise (FR-157h). **Code:** new `terminal/term_translate.py` (`TerminalTranslator` identity base, `VT52Translator`, `ADM3ATranslator`, `make_translator`, `TERMINAL_TYPES` — GUI-free, CR-014); `terminal/vt100_engine.py` (`VT100Engine` gains a `terminal_type` ctor param, `terminal_type` property, `set_terminal_type`; `feed` routes bytes through the active translator); `utils/config_handler.py` (`terminal_type` default `VT100`); `gui/config_dialogs.py` (`SerialConfigDialog` Terminal Type dropdown); `gui/terminal_view.py` (`encode_key` gains a `terminal_type` arg with VT-52/ADM-3A cursor-key override tables; `keyPressEvent` passes the engine's type); `gui/mw_config.py` (`_apply_terminal_type` applied from `load_config` and the Serial dialog save); `lang/lang_*.txt` (new `config.serial.terminal_type` label, all 12 languages). **Tests:** new `tests/test_term_translate.py` (VT-52/ADM-3A sequence→VT-100 translation, split-chunk reassembly, direct-address bias, unknown/malformed robustness, factory/identity); `tests/test_vt100_engine.py` (terminal_type default; VT-52 direct-address end-to-end; ADM-3A clear-screen; `set_terminal_type` switch); `tests/test_terminal_view.py` (VT-52/ADM-3A arrow overrides; default stays VT-100); `tests/test_config_handler.py` (default terminal_type). Full unit suite green; `ruff`/`mypy` clean; views regenerated, 0 stale tags, all new IDs covered. **Integration (HIL):** the feature only changes how the Terminal Window interprets bytes/keys; a peer-driven check needs a remote that actually emits VT-52/ADM-3A, so no automated HIL case was added — a manual bench case (MT-W15) documents verifying it against ADM-3A/VT-52 software when hardware is available; bench run pending. **Docs:** user manual (Config → Serial table + note, Reference: Default Settings) and manual test plan/scorecard updated and versioned; `src/version.txt` → `2.22.0`. |
| 2.21.0 | 2026-07-06 | Software Engineer | Added a floating Macro Window of configurable keystroke buttons. **Added:** UIR-096 (a "Macros" checkbox in the Terminal Window control row, left of Local Echo, that shows/hides the Macro Window); UIR-097 (the Macro Window — a floating, resizable `Qt.Tool` palette titled "Macros" that shows up to ten configured buttons in a reflowing `FlowLayout`; only slots with both a label and a keystroke script appear, and the set refreshes when the config changes); UIR-098 (the Macro Buttons Configuration Dialog — Config > Macro Buttons — a modal, tabbed dialog with one tab per button, "Button 1"…"Button 10", each tab holding a Label field, a multi-line Keystrokes editor in the boot-sequence directive language, and a Test button); FR-162 (clicking a macro button, or its Test button, executes that slot's script on the Terminal Port using the same `SEND`/`SENDRAW`/`WAIT`/`WAITFOR` parser and worker-thread execution as the boot sequence, guarded on the Terminal Port being open); FR-021b (Config > Macro Buttons opens the dialog; Save persists only the macro settings to the loaded config file and refreshes the live window); FR-164 (the checkbox toggles the window's visibility, closing the window hides it and clears the checkbox, and the window geometry persists in QSettings). **Design note:** macro definitions are per-configuration and live in the serial JSON as `macro_<n>_label`/`macro_<n>_seq` (all default empty), like `boot_sequence`; the window geometry is UI/session state in `WindowState`. The keystroke-script parser (`terminal/boot_sequence.py`) is reused unchanged, and `mw_remote.run_boot_sequence` was refactored to share a new `_execute_sequence` helper with the macro runner. **Code:** `gui/flow_layout.py` (new `FlowLayout`); `gui/macro_window.py` (new `MacroWindow`); `gui/config_dialogs.py` (new `MacroConfigDialog`); `gui/terminal_window.py` (`chk_macros` checkbox); `gui/mw_remote.py` (`_toggle_macro_window`/`_show_macro_window`/`_on_macro_window_hidden`/`_refresh_macro_buttons`/`run_macro_script`/`_run_sequence_logic`/`_execute_sequence`); `gui/mw_config.py` (`menu_macro_config`); `app.py` (menu action, `macro_win` state, close/retranslate); `utils/config_handler.py` (macro defaults); `lang/*.txt` (macro strings). **Tests:** `tests/test_macros.py`; HIL `integration/test_terminal_window.py::test_macro_button_sends_keystrokes_over_serial` (MT-W14). |
| 2.20.0 | 2026-07-06 | Software Engineer | Added a configurable Terminal Window font. **Added:** UIR-069 (a "Font…" button in the Terminal Window control row, right of the Autoscroll checkbox, opens the standard `QFontDialog`; the chosen font applies immediately to the Receive view — the character-cell grid reflowing to the new cell metrics, FR-091a — and is persisted across sessions in the QSettings UI-preferences store, cf. FR-004/FR-124, independently of the loaded serial configuration; default is a monospaced Courier New). **Design note:** the terminal font is a global UI preference, so it lives in `WindowState` (QSettings) alongside geometry/language/filter — not the per-config serial JSON — round-tripped via `QFont.toString()`/`QFont.fromString()`. **Code:** `gui/window_state.py` (`default_terminal_font` helper; `terminal_font` property); `gui/terminal_view.py` (`TerminalView` constructor accepts an optional font; `_apply_font` metrics helper; `set_font`/`current_font`); `gui/terminal_window.py` (`font_callback` ctor param; `btn_font` "Font…" button; `_build_font_dialog`/`_on_font` open a `QFontDialog`; `set_terminal_font`) — the dialog is forced non-native and carries a scoped `_FONT_DIALOG_STYLE` that overrides the app-wide Material stylesheet's fixed 36px `QListView` height, which would otherwise collapse the dialog's family/style/size lists to a single unusable row; `gui/mw_remote.py:show_terminal` (passes `font_callback=_save_terminal_font` and applies the saved font on first open; new `_save_terminal_font`); `lang/lang_*.txt` (new `terminal.font`/`terminal.font_dialog_title` keys, all 12 languages); `src/version.txt` → `2.20.0`. **Tests:** `tests/test_terminal_view.py` (`test_constructor_accepts_font`, `test_set_font_updates_cell_metrics_and_paints`); new `tests/test_window_state.py` (font default/round-trip/persistence-across-instances); `tests/test_gui_smoke.py::test_font_dialog_lists_survive_app_theme_height` (regression: the dialog's lists stay usably tall under an app-wide `QListView { height: 36px }` rule). **Integration (HIL):** two no-peer widget-tree/look-and-feel assertions added to the `visual` tier — `integration/test_visual_assertions.py::test_terminal_control_row_has_font_button` and `::test_font_dialog_lists_usable_under_material_theme` (the latter builds the dialog against the real applied Material theme and asserts its family/style/size lists are not collapsed — a bench-independent guard for the styling fix). No peer-driven (serial/X-Modem) case was needed: the feature touches no protocol or GUI-over-serial flow. Full unit suite green; `ruff`/`mypy` clean; views regenerated, 0 stale tags, UIR-069 covered. **Docs:** manual test plan and scorecard updated (new MT-W13); user manual (Terminal Window section + Reference: Default Settings) updated. |
| 2.19.0 | 2026-07-05 | Software Engineer | Two defect-investigator-workflow fixes. **Bug fix — reconnecting while already connected:** pressing **Connect** while the Terminal Port was already open re-attempted the open, which fails (the OS won't grant a second handle) and desynced the connected flag from the real port state — the flag went false while the port stayed open (leaked), locking the user out of ever reconnecting without restarting the app. `do_connect` now checks `terminal_connected` first and, if already open, reports "Terminal port is already open" on the status bar and takes no further action, satisfying FR-030's existing "if it is not already open" clause that the implementation had never actually enforced. **Added:** `status.terminal_already_open` (all 12 languages). **Code:** `gui/mw_remote.py:do_connect`. **Tests:** `tests/test_gui_smoke.py::test_connect_when_terminal_already_open_notifies_and_takes_no_action` (`Verifies: FR-030`). **Feature — misconfigured remote-command diagnosis and pre-flight test:** a misconfigured Send to Remote / Receive from Remote command (FR-087) previously produced a generic "Transfer failed" only after the full 60-second X-Modem handshake wait, with no hint that the command itself was the problem. **Added:** FR-159 (a handshake that gets zero response bytes at all — as opposed to a failure once the transfer was underway — reports a distinct "No response from remote for `<name>` - check the Send to Remote / Receive from Remote command in Config > General" error instead of the generic one), FR-160 (the initial handshake wait is now a dedicated, configurable `xfer_handshake_timeout` — default 10s, UIR-093 — independent of and much shorter than the old fixed 60s, and independent of the NFR-003p per-packet retry timeouts used once a transfer is underway), FR-161/UIR-094/UIR-095 (a "Test" button beside each of the Receive from Remote and Send to Remote fields in Config > General: requires an active connection like a real transfer, launches the *currently typed* command with `$1` replaced by a fixed placeholder filename, waits up to the handshake timeout for any response, reports success or the FR-159 diagnosis, then cancels — X-Modem CAN — anything the command may have launched; no file is ever transferred). **Design note:** the Test button is implemented as a bounded `QTimer` poll on the GUI thread rather than a worker thread plus Qt signal — the wait is short and cancellable by construction, and this sidesteps the class-level-signal-connection bookkeeping a repeatedly created/destroyed dialog would otherwise need (see the prior "gui: fix signal-pollution from class-level Qt signals" fix). **Code:** `terminal/xmodem.py` (`XModem.__init__` gains `handshake_timeout`/`no_response`; `send_file`/`receive_file` use the configurable timeout and set `no_response` only on a genuine zero-byte timeout, never on cancellation); `mw_transfers.py:_handshake_timeout`; `mw_transfer_batches.py` (`_send_one_to_remote`/`_recv_one_to_host` pass the timeout and capture `no_response`; both batch drivers pick the FR-159 message on a non-cancelled failure); `app.py` (`_last_xmodem_no_response` state, reset per batch); `gui/config_dialogs.py` (new `xfer_handshake_timeout` field; a new declarative "button" field type; `GeneralConfigDialog._test_recv_remote_cmd`/`_test_send_remote_cmd`/`_start_command_test`/`_test_begin_listen`/`_test_poll`/`_finish_command_test`/`_set_test_buttons_enabled`). **Tests:** `tests/test_xmodem.py` (no-response set on genuine timeout, unset on cancel, unset on success, configurable/default timeout); `tests/test_gui_smoke.py` (upload/download no-response error wording, Test button connection guard, success, and no-response outcomes; updated `test_general_config_remote_group_first` for the two new label-less button rows); test-double `XModem` stubs across `test_gui_smoke.py`/`test_conflict_resolution.py`/`test_filename_validation.py` updated to accept `handshake_timeout` and expose `no_response` (their previous signatures raised `TypeError`/`AttributeError` against the evolved real class, which an unmocked `QMessageBox.critical` in the exception path turned into a hang under the offscreen Qt platform — caught by running the full suite, not by the targeted new tests alone). Full unit suite green (470 tests); `ruff`/`mypy` clean; views regenerated, 0 stale tags; UIR-093 joins UIR-049/UIR-052 as an expected manual-only (untested) config-field entry. **Docs:** manual test plan v1.25→1.26 (MT-G04 extended, new MT-G11/MT-T15); scorecard v1.25→1.26 to match; user manual (Config → General command-template table, Other Settings table, Troubleshooting, Reference: Default Settings) updated for both the Connect notification and the handshake timeout/Test button; `src/version.txt` → `2.19.0`. |
| 2.18.1 | 2026-07-05 | Requirements Checker | Code/requirements traceability alignment audit (`Workflows/code-requirements-align.md`). **No application behaviour change** except one wording correction (below); the full unit suite passes (new cases only added), `ruff`/`mypy` clean, views regenerated with 0 stale tags. **Tooling fix (Major):** `tools/traceability_sync/parser_code.py`'s `RequirementExtractor` only visited `FunctionDef`/`ClassDef` AST nodes, so a `Satisfies:`/`Verifies:` tag written in a **module-level docstring** was silently invisible to `code_to_requirements.md`/`.json` and to `agent_toolset.py`'s mechanical trace — the direct cause of a false "orphan" reading for CR-014. Added `RequirementExtractor.visit_Module` (recorded under the synthetic name `"<module>"`); this surfaced 23 previously-hidden mappings, including CR-014 (`file_filter.py`, `i18n.py`, `transfer_history.py`) and FR-130 (`file_filter.py`). New `test_scan_codebase_reads_module_level_docstring_tag` in `tests/test_traceability_tooling.py`. **Wording correction:** FR-074, FR-098, FR-104, FR-117c's status-bar text read "Terminal port not open - cannot …" in the SRS, but the v2.13.0 entry above already records that this wording was unified to "Terminal port not connected - cannot …" across the app — the SRS text for these four requirements was simply never updated to match. Corrected to "not connected" (matches the shipped strings and existing tests); updated the parallel wording in `docs/manual_test_plan.md` (MT-R02, MT-W07, MT-F07). **Stale Source citations corrected (no code change):** FR-061/070/071/072 (`app.py:setup_layout`/`__init__`), FR-157a–h (`vt100_engine.py:feed`/`__init__`, dropping an inconsistent class-qualified form), UIR-015/016 (`app.py:setup_toolbar`), CR-014 (dropped a stale "CLAUDE.md" reference — this repo has no CLAUDE.md, only `AGENTS.md` — and listed all 5 real locations), CR-011 (`config_handler.py`/`config_dialogs.py`, was misattributed to `app.py`), CR-012 (added the missing `app.py:main` citation). **Missing `Satisfies:` tags added (behaviour already existed, just untagged):** FR-033 (`serial_manager.py:open_port`), UIR-063 (`terminal_view.py` `__init__`/`keyPressEvent`), CR-012 (`app.py:main`), UIR-032/033 (`SerialConfigDialog` range extended), UIR-089/090 (`GeneralConfigDialog` range extended; `mw_transfers.py:_xmodem_1k_enabled`/`_issue_remote_cmd`), FR-157c (`terminal_view.py:_colour`). **New tests for genuine coverage gaps:** `tests/test_cpm_parser.py` — DR-002, DR-003, DR-010, DR-012, DR-020, DR-022, DR-024, DR-025, DR-026 (CP/M `DIR`-output parser edge cases: drive-prompt/`NO FILE` filtering, drive-prefix stripping, whitespace normalisation, dict-key dedup, case preservation, malformed-input tolerance, empty-result, irregular spacing/colons/line-endings); `tests/test_serial_manager.py` — FR-001, FR-002 (status flags default false); `tests/test_gui_smoke.py` — FR-054 (shared-port disconnect clears the Transport flag), FR-056 (Transport-close-failure error dialog). **Tagging-only manual-plan fix:** MT-P02/MT-G01 now also cite FR-020/FR-021 (the manual step already exercised the menu-opens-dialog action; only the ID column was missing it); scorecard rows updated to match. Unit-test coverage: 194→207 of 420 requirements now have a verifying test. **Docs:** views regenerated (`generate_views.py`); manual test plan v1.24→1.25 / scorecard v1.24→1.25; user manual version line bumped (no content change — no user-visible behaviour changed); `src/version.txt` → `2.18.1`. |
| 2.18.0 | 2026-07-01 | Requirements Checker | Requirements-quality pass (ISO/IEC/IEEE 29148) focused on the v2.17 VT-100 cluster, plus one behavioural addition (terminal grid reflow). **Feature — terminal reflow:** the Terminal Window's character-cell grid now **reflows to the window size** — on resize the visible column/row counts are recomputed from the viewport and the emulator screen is resized to match (down to a 20×5 minimum). The serial link has no size-negotiation channel, so the remote is **not** notified; full-screen CP/M software that assumes a fixed 80×24 page continues to address that page (documented limitation). **Added:** FR-091a (grid reflow-to-window), FR-157a–FR-157h (the v2.17 monolithic FR-157 decomposed for atomicity per 29148 *singularity*: cursor positioning/movement + show/hide (a), erase-in-line/-display (b), SGR attributes and ANSI colour (c), scrolling region (d), tab stops (e), DEC line-drawing charset (f), UTF-8/raw-8-bit byte decoding with U+FFFD replacement and remote `ESC%G`/`ESC%@` mode switching (g), and unsupported/malformed-escape robustness (h)). **Modified (clarity/verifiability/consistency):** FR-004 (verification method D→T — geometry persistence is unit-tested); FR-036 (stale plain-text "receive text area" model deferred to the FR-091 VT-100 screen model); FR-090/FR-093 (disambiguated the retained `_rx_buffer` from the rendered screen — local echo goes only to the screen); FR-091 (scrollback depth quantified as ≥1000 lines; reflow reference; render-verification note); FR-131 (debounce "~150 ms" fixed to a normative 150 ms); FR-157 (reduced to a parent citing FR-157a–h; DECCKM cross-referenced to FR-158); FR-158 (added the exhaustive key→byte mapping table; modifier-only/unmapped keys transmit nothing); NFR-002 (the "e.g." alias list replaced with the exhaustive, ordered normative key/alias set); UIR-062 (scrollback depth quantified; v2.17 tag added); UIR-064 ("Receive text area"→"Receive view"); UIR-065 ("disabled"→"unchecked (local echo off)"); DR-041 (version now 2.18.0). **Metadata fix:** FR-155/FR-156 (removed requirements) Priority corrected from `Mandatory` to `—`, matching the FR-035/UIR-043 removal convention (they no longer pollute the untested-requirement metric). **Correctness note:** FR-157f was corrected during implementation — the `pyte` backend substitutes DEC line-drawing glyphs only under raw 8-bit decoding, not the default UTF-8; the requirement now states this accurately. **Resolved:** OI-32 (v2.17 terminal-cluster requirement-quality issues — ambiguity/verifiability/consistency/metadata), OI-33 (FR-036 left describing the superseded plain-text terminal model after the v2.17 migration). **Code:** `gui/terminal_view.py` (new `grid_size_for`, `_MIN_COLS`/`_MIN_ROWS`, `TerminalView.resizeEvent`/`_reflow_to_viewport`); `terminal/vt100_engine.py` (Satisfies tags extended to FR-157a–h / FR-091a; U+FFFD note; module-comment refresh). **Tests:** `tests/test_vt100_engine.py` (re-tagged to the FR-157 sub-IDs; new line-drawing (8-bit), scrolling-region, unknown-escape-robustness, and UTF-8-replacement cases; resize case tagged FR-091a); `tests/test_terminal_view.py` (new `grid_size_for` and reflow cases, FR-091a); re-tagged existing tests to FR-108a/FR-108b, FR-113a/FR-113b, FR-137c/FR-137e, FR-146b. Full unit suite green; `ruff`/`mypy` clean; views regenerated; 0 stale tags. **HIL:** no HIL-code change required; add manual case MT-W12 (grid reflow) — the terminal-window HIL cases still read the screen from the engine. Bench run pending hardware. **Docs:** views regenerated (`generate_views.py`); manual test plan v1.24 / scorecard v1.24 add MT-W12; user manual §13 notes the resizable terminal; `src/version.txt` → `2.18.0`. |
| 2.17.0 | 2026-07-01 | Software Engineer | Interactive VT-100 terminal. **Feature:** the Terminal Window is now a functional VT-100/ANSI terminal instead of a glass-TTY text log. Received bytes are interpreted by a new emulator engine (cursor positioning, erase-in-line/-display, SGR attributes incl. ANSI colour, scrolling region, tabs, DEC line-drawing charset) and rendered on an 80×24 character-cell screen with scrollback, so full-screen CP/M software (editors, etc.) displays correctly. The separate transmit field and Send button are **removed**: the operator types directly into the receive area and each keystroke is transmitted live, encoded to its VT-100 byte sequence (printable as UTF-8; Enter → configured EOL; Backspace/Tab/Escape; arrow/Home/End/PgUp-Dn/Insert/Delete/F1–F12 → CSI/SS3; Ctrl-combinations → C0 control bytes). Application-cursor-key mode (DECCKM) is not tracked — arrows always send normal-mode sequences. **Added:** FR-157 (VT-100/ANSI escape-sequence interpretation and rendering via the `pyte`-backed engine; UTF-8 default decode with a raw 8-bit mode), FR-158 (per-keystroke key→VT-100 byte encoding). **Modified:** FR-091 (received data interpreted as a VT-100 stream and rendered on a character-cell screen, was plain-text append), FR-092/FR-093 (transmit buffer/local echo now via the keystroke path and the engine), FR-094 (EOL applies to boot/command sends and the Enter key; the transmit-field exception removed), FR-095 (Clear resets the screen incl. scrollback), FR-096 (live per-keystroke input replaces the Send button/transmit field), FR-098 (port-closed guard on typing), UIR-061 (character-cell terminal view), UIR-062 (scrollback + autoscroll), UIR-063 (receive view is non-editable but keyboard-focusable for input), UIR-067 (no transmit field/Send button; input hint label), DR-041 (application/SRS version now `2.17.0`). **Removed:** FR-155 (empty-field bare-EOL — Enter now sends the EOL directly), FR-156 (transmit-field caret notation — real Ctrl keys now). **Architecture:** `terminal/vt100_engine.py` added to the `terminal/` layer; `pyte` is a new pure-Python, GUI-free runtime dependency (CR-014 holds). The receive path decodes for the receive/capture buffers on the daemon thread but feeds the engine only on the GUI thread (via `term_write`, now carrying bytes), keeping the emulator single-threaded (NFR-001/NFR-004). **Code:** new `terminal/vt100_engine.py` (`VT100Engine` over `pyte`); new `gui/terminal_view.py` (`TerminalView` grid renderer + `encode_key`/`keyPressEvent`); `gui/terminal_window.py` (receive area → `TerminalView`; Send box/`send_text`/`_parse_send_text` removed; input hint label; `render_screen`/`set_eol`/`focus_input`); `gui/mw_remote.py` (`handle_terminal_key`; `handle_terminal_recv` tees raw bytes; `show_terminal` wires key callback/EOL/focus); `app.py` (`_term_engine`; `term_write` → `Signal(bytes)`; `_on_term_write` feeds engine + renders); `serial_manager.py` (`_read_loop` delivers raw bytes); `pyproject.toml` (`pyte>=0.8.2`); all `lang/lang_*.txt` (`terminal.send` → `terminal.input_hint`, in all 12 languages). **Tests:** new `tests/test_vt100_engine.py` and `tests/test_terminal_view.py`; `tests/test_gui_smoke.py` (recv tee, keystroke transmit, `handle_terminal_key` send/echo/guard) and `tests/test_serial_manager.py` (raw-byte read loop) updated; obsolete caret-notation/Send-box tests removed. **HIL:** `integration/test_terminal_window.py` reads the screen from the engine and adds an interactive keyboard case (MT-W06); **bench run pending hardware**. **Docs:** views regenerated (`generate_views.py`); architecture A2 module list + diagram updated; user manual §13 rewritten; manual test plan + scorecard updated and versions bumped; `src/version.txt` → `2.17.0`. |
| 2.16.0 | 2026-06-27 | Software Engineer | Configurable boot-into-CP/M sequence. **Feature:** some remote computers do not boot straight into CP/M, so the user can now define a per-config **boot sequence** of keystrokes that drives the machine into CP/M. The sequence is a small newline-separated script (one directive per line; blank lines and `#` comments ignored) with four directives: `SEND <text>` (text + configured EOL), `SENDRAW <hh …>` (raw hex bytes, no EOL — for control keys such as `03`/Ctrl-C or `1B`/ESC), `WAIT <seconds>`, and `WAITFOR <text> [seconds]` (capture Terminal output until the text appears or a default-10 s timeout elapses). The sequence runs only when needed: **automatically** when the post-connect probe finds no CP/M drive prompt and a sequence is configured (it runs the sequence, then re-probes once; only if that still fails is the Remote Filesystem Unavailable dialog shown), and **manually** via a new "Boot into CP/M" button in the Terminal Window. An empty `boot_sequence` disables the feature, leaving the v2.15.0 probe-failure behaviour unchanged. **Added:** FR-047 (boot-sequence definition, grammar, and worker-thread execution on the Terminal Port — NFR-004), FR-048 (automatic recovery on probe failure: run the sequence and re-probe once, at most once per Connect, before FR-044's dialog), FR-049 (manual execution via the UIR-068 button, re-probing on completion and reporting failure in the status bar without the modal dialog), UIR-059 (multi-line "Boot Sequence" field in the General Configuration Dialog, `boot_sequence`, default empty), UIR-068 ("Boot into CP/M" button in the Terminal Window control row, disabled when `boot_sequence` is empty and re-evaluated when the config changes while the window is open). **Modified:** FR-044 (boot-sequence recovery inserted ahead of the Remote Filesystem Unavailable dialog), UIR-044 (the ungrouped general settings now include "Boot Sequence"), DR-041 (application/SRS version now `2.16.0`). **Code:** new `terminal/boot_sequence.py` (`parse_boot_sequence` — pure, no GUI imports, CR-014); `gui/config_dialogs.py` (new `multiline` field type via `QPlainTextEdit`; `GeneralConfigDialog` boot-sequence field); `utils/config_handler.py` (new `boot_sequence` default `""`); `gui/mw_remote.py` (`run_boot_sequence` worker; `_do_connect_probe_logic` auto-recovery; `show_terminal` wires the button callback + initial enabled state; settings-save refreshes the button); `gui/terminal_window.py` (`Boot into CP/M` button + `set_boot_enabled`); `gui/mw_base.py` (signal declarations as needed); all `lang/lang_*.txt` (new `config.general.boot_sequence`, `terminal.boot`, `status.boot_running`, `status.boot_failed` keys, in all 12 languages). **Tests:** new `tests/test_boot_sequence.py` (parser directives, comments/blank lines, hex/EOL handling); `tests/test_i18n.py` (new keys resolve); `tests/test_gui_smoke.py` (auto-recovery on probe failure, button enable/disable by config, empty-sequence no-op). **Docs:** views regenerated (`generate_views.py`); manual test plan + scorecard add boot-sequence cases and bump their versions; `src/version.txt` → `2.16.0`. |
| 2.15.0 | 2026-06-27 | Software Engineer | Post-connect remote-file-system probe. **Feature:** when Connect leaves both the Terminal and Transport ports open, the application now sends an EOL on the Terminal Port and waits for a CP/M drive prompt to confirm the remote file system is reachable. On a prompt it sets the drive drop-down to the remote's current drive and populates the Remote Files list automatically; with no prompt it sends one more EOL (a single retry) and, if still silent, shows a modal dialog reporting that the remote file system cannot be accessed, offering three buttons in the fixed left-to-right order **Abort** (close the port(s) per the Disconnect behaviour and clear the list), **Continue** (no action, ports stay open), **Terminal** (open the Terminal Window to debug, ports stay open). **Added:** FR-041 (probe on EOL after both flags connected, on a worker thread per NFR-004, status "Checking remote file system"), FR-042 (drive prompt → set drop-down to the returned letter and populate the list per FR-074–FR-079), FR-043 (no prompt → resend EOL once), FR-044 (still no prompt → modal UIR-092), FR-045 (the Abort/Continue/Open actions), FR-046 (probe only when both ports connected), UIR-092 (Remote Filesystem Unavailable dialog — modal, three buttons in fixed order Abort/Continue/Open, overriding the two-button UIR-075 arrangement), DR-033a (drive-prompt letter-extraction routine used by the probe). **Modified:** DR-033 (drive-prompt detection generalised from a bare `X>` prefix to also accept ZCPR-style user-area digits around the drive letter — e.g. `A0>`, `4A>` — so both the new probe and the existing drive-change detection (FR-101/FR-102) recognise ZCPR/NZCOM prompts; path-style `A0:BASE>` prompts remain out of scope), §6 intro (test-coverage note extended to DR-033a and ZCPR prompts), DR-041 (application/SRS version now `2.15.0`). **Resolved:** OI-30 (drive-prompt detection was blind to ZCPR user-area prompts). **Code:** `terminal/cpm_parser.py` (new `_DRIVE_PROMPT_RE`; `has_drive_prompt` re-implemented on the regex; new `drive_prompt_letter`); `gui/mw_remote.py` (`do_connect` starts the probe; new `_do_connect_probe_logic`, `_on_connect_probe_ok`, `_on_connect_probe_failed`); `app.py` (new `connect_probe_ok`/`connect_probe_failed` signals + wiring); `gui/mw_base.py` (mixin signal declarations); new `gui/remote_unavailable_dialog.py:RemoteUnavailableDialog`; all `lang/lang_*.txt` (new `status.checking_remote_fs`, `dialog.remote_unavailable.title`, `dialog.remote_unavailable.body`, `button.abort`, `button.terminal` keys, in all 12 languages). **Tests:** `tests/test_cpm_parser.py` (ZCPR/path-style `has_drive_prompt` cases; `drive_prompt_letter` extraction cases); `tests/test_i18n.py` (new keys resolve); `tests/test_gui_smoke.py` (probe scheduling/skip, prompt-success sets drive + refreshes, retry-then-success, and the Abort/Continue/Open dialog actions). **Docs:** views regenerated (`generate_views.py`); manual test plan v1.21 / scorecard v1.21 add MT-C11/MT-C12(a–c)/MT-C13; `src/version.txt` → `2.15.0`. |
| 2.14.1 | 2026-06-27 | Software Engineer | Test-traceability pass — documented two X-Modem robustness behaviours that the suite already verified but no requirement had specified, surfaced by the new `Verifies:` test-traceability tooling (the `requirements_to_tests` coverage view / `agent_toolset.py --coverage`). **No application behaviour change** — code unchanged; the behaviours already existed, only their requirements and traceability were missing. **Added:** NFR-003p (when a transmitted packet is NAK'd/unanswered the sender retransmits the same packet for a bounded number of attempts (10) and aborts with failure rather than retransmitting indefinitely — was verified by `test_send_file_aborts_after_nak_exhaustion` but unspecified), NFR-003q (an EOT before any data packet is accepted as a valid empty transfer — ACK the EOT, write a zero-length file — verified by `test_receive_file_empty_transfer_writes_empty_file`). **Resolved:** OI-29 (the two unspecified-but-tested behaviours). **Modified:** DR-041 (application/SRS version now `2.14.1`); §9 traceability summary and the §11 deferred-constraints row extended `NFR-003a–NFR-003o` → `NFR-003a–NFR-003q`. **Code (traceability only):** `terminal/xmodem.py` `Satisfies:` tags extended — `send_file` +NFR-003p, `receive_file` +NFR-003q, class docstring +NFR-003p/NFR-003q. **Tests:** `tests/test_xmodem.py` — the two tests above gained `Verifies:` tags for the new IDs (no logic change). **Docs:** views regenerated (`generate_views.py`); manual test plan v1.20 / scorecard v1.20 NFR-003 range extended to `NFR-003q`; `src/version.txt` → `2.14.1`. |
| 2.14.0 | 2026-06-27 | Software Engineer | Source-file size reduction — decomposed the oversized `MainWindow` (`app.py`) into cohesive GUI mixin modules, and synced the requirements traceability to the new file layout. **No application behaviour change** — the full test suite (341 tests) passes unchanged; `ruff`/`mypy` clean. **Why:** `app.py` was a 3041-line, 124-method outlier (~46% of the codebase); loading it to edit one feature was expensive for humans and small local LLMs alike. **Result:** `app.py` drops to 899 lines (a `QMainWindow` shell: `__init__`, i18n/setup/status, the viewer + shared `_confirm_dialog` helpers, `closeEvent`, `main`, `build_viewer_args`); every source module is now under ~360 lines. **Code:** `MainWindow` is now assembled from nine mixins under `gui/` — `mw_transfers.py` (`_TransfersMixin`), `mw_transfer_batches.py` (`_TransferBatchesMixin`), `mw_transfer_guards.py` (`_TransferGuardsMixin`), `mw_file_panes.py` (`_FilePanesMixin`), `mw_context_menu.py` (`_ContextMenuMixin`), `mw_remote.py` (`_RemoteMixin`), `mw_backup_restore.py` (`_BackupRestoreMixin`), `mw_config.py` (`_ConfigMixin`), `mw_history.py` (`_HistoryMixin`) — plus `gui/mw_base.py` (`MainWindowMixinBase`), a TYPE_CHECKING-only base (empty at runtime) declaring the shared `MainWindow` surface so the mixins type-check cleanly. `EOL_MAP` moved to `mw_remote.py`; the `from cpm_fm.app import MainWindow`/`main`/`build_viewer_args` surface is unchanged. Four `# type: ignore[arg-type]` cover `self`-as-`QObject` parent calls in typed mixin methods (`QTimer`/`QMessageBox`). **Modified (requirements — citations only, no requirement text changed):** the Source-column `impl.` citations of ~120 requirements repointed from `app.py:*` to their new `gui/mw_*.py` modules; CR-007 (reworded — a single large component may be composed from several cooperating modules, e.g. the main window from the `gui/mw_*.py` mixins); NFR-001 (transfer threads now `mw_transfer_batches.py`); DR-041 (application/SRS version now `2.14.0`). **Traceability fixes (pre-existing, surfaced during the sync):** DR-046 — escaped an unescaped `|` in the body's CP/M delimiter list that had made the row a malformed 8-cell table row, silently mis-parsed since before this change; CR-015 — added the missing `CR-015` `Satisfies:` tag to `config_dialogs.py:ConfigDialog` (its own docstring already declared it); normalized the DR-045/DR-047/FR-148 citation styles to `basename:method`. **Tests:** behaviour-preserving move — ~21 `monkeypatch` targets repointed to the new modules (`XModem`, `FileConflictDialog`, `FilenameValidationDialog`, `time.sleep`, `CPMParser.parse_dir_output`, `FileActionDialog`, `QFileDialog`, `AboutDialog`, `ManualDialog`, `Serial`/`GeneralConfigDialog`); no test logic or count changed. **Docs:** requirement views regenerated (`generate_views.py`); `src/version.txt` → `2.14.0`. |
| 2.13.2 | 2026-06-25 | Software Engineer | Bug fix — cancelling a transfer left the progress dialog stuck open. **Bug:** the v2.13.1 abort pushed the CAN (0x18) sequence out with `serial.Serial.flush()`, which busy-waits on `out_waiting` with no timeout. When the transmit line could not drain — the aborting remote stops asserting CTS (RTS/CTS) or sends XOFF (XON/XOFF), so flow control holds the queued bytes — `flush()` blocked forever and hung the transfer worker thread. The worker never reached `_finish_cancelled_batch`, so the `transfer_cancelled` signal was never emitted and the modal Transfer Progress Dialog (no close button; Cancel already disabled and showing "Cancelling…") stayed on screen with no way to dismiss it. Reported on host→remote, where the large queued packet data makes the stall most likely; the single shared `_abort` path means it could affect receive cancellation too. **Fix:** `_abort` now waits for the CAN bytes via a new **bounded** `_drain_tx(timeout=1.0)` that polls `out_waiting` until it reaches 0 or the timeout elapses, instead of the unbounded `flush()`; ports/stubs without `out_waiting` fall back to a single best-effort `flush()`. The abort always returns, so the worker always signals completion and the dialog is always torn down; any CAN bytes still queued when the wait elapses are transmitted by the OS in the background while the (shared transport) port stays open, so the remote still receives the abort. **Also (responsiveness):** the remaining plain `time.sleep` waits on the transfer worker thread — the FR-089 post-launch handshake delay (`_send_one_to_remote`/`_recv_one_to_host`), the FR-109 between-files settle (`_wait_for_terminal_idle`), and the FR-145 pre-upload remote listing read (`_capture_terminal_response` when called from `_fresh_remote_names`) — now poll the cancel flag via a new `_cancellable_sleep` helper and wake early, so a Cancel during any of them is observed promptly instead of after the full delay (up to ~10s). The standalone Remote-list refresh / drive-change reads keep the non-cancellable behaviour so they never observe a stale transfer-cancel flag. **Added:** OI-26. **Modified:** FR-120 (the abort's wait for the CAN to drain is bounded so a flow-control stall cannot hang the worker and freeze the dialog open), NFR-003 (the CAN-drain wait is time-bounded via `_drain_tx`, not an unbounded `flush()`), DR-041 (application/SRS version now `2.13.2`). **Code:** `terminal/xmodem.py:_abort` (`flush` → `_drain_tx`), new `terminal/xmodem.py:_drain_tx`; new `app.py:_cancellable_sleep`, with the FR-089/FR-109/FR-145 worker-thread waits (`_send_one_to_remote`, `_recv_one_to_host`, `_wait_for_terminal_idle`, and `_capture_terminal_response`/`_fresh_remote_names` via a new `cancellable` flag) routed through it. **Tests:** `tests/test_xmodem.py` (the in-memory fake serial gains an `out_waiting` property; `test_cancel_flushes_serial_in_both_directions` updated for the bounded drain; new `test_abort_does_not_hang_when_tx_cannot_drain` runs the cancel abort against a serial whose `out_waiting` never reaches 0 and asserts `send_file` returns promptly rather than hanging); `tests/test_gui_smoke.py` (new `test_cancellable_sleep_returns_immediately_when_cancelled`, `test_cancellable_sleep_completes_when_not_cancelled`, `test_wait_for_terminal_idle_returns_early_on_cancel`, `test_capture_terminal_response_cancellable_bails_early`; the `_capture_terminal_response` stubs updated for the new optional `cancellable` argument). `src/version.txt` → `2.13.2`. |
| 2.13.1 | 2026-06-24 | Software Engineer | Bug fix — flush the serial port on transfer cancellation. **Bug:** when a file transfer was cancelled, the X-Modem abort transmitted the CAN (0x18) sequence to the remote but never flushed the Transport Port, so the partially-transmitted packet still queued for transmit kept draining to the remote and incoming bytes kept arriving — the transfer appeared to keep going until the buffers emptied. The abort now flushes the Transport Port in **both** directions: it discards the queued transmit data (`reset_output_buffer`), sends the CAN and blocks until it is actually transmitted (`write` + `flush`), then discards anything the remote was still sending (`reset_input_buffer`); the ordering keeps the CAN itself from being discarded. Because the abort is the single point every cancellation path funnels through (handshake, between packets, mid-retransmit, and during receive), this covers both send and receive cancellation. **Modified:** FR-120 (the abort flushes the Transport Port transmit and receive buffers so the cancel takes effect at once), NFR-003 (CAN abort now also flushes both serial buffers, with the transmit-discard / CAN-flush / receive-discard ordering), DR-041 (application/SRS version now `2.13.1`). **Code:** `terminal/xmodem.py:_abort` (`reset_output_buffer` → `write(CAN*3)` → `flush` → `reset_input_buffer`). **Tests:** `tests/test_xmodem.py` (the in-memory fake serials gain `reset_output_buffer`/`flush`; new `test_cancel_flushes_serial_in_both_directions` asserting the cancel path resets the output buffer, flushes, and resets the input buffer). `src/version.txt` → `2.13.1`. |
| 2.13.0 | 2026-06-22 | Software Engineer | XMODEM-1K branch: selectable 1K transfer mode, X-Modem receive-robustness fixes, configurable per-port serial read timeouts, per-group configuration save, and a backup/messaging fix pass. **Feature — selectable XMODEM-1K:** host→remote sends can now use 1024-byte STX framing with dedicated CP/M launch commands. **Added:** UIR-089 ("Use XMODEM-1K" checkbox in the General dialog's Remote group, `xmodem_1k` OFF/ON default OFF), UIR-090 ("Receive from Remote (1K)"/"Send to Remote (1K)" command fields `recv_remote_cmd_1k`/`send_remote_cmd_1k`, each blank-falls-back to its standard counterpart). **Modified:** NFR-003 (1024-byte STX send framing when 1K mode is on; CRC-first receive handshake in 1K mode so a 1K-capable sender switches to 1024-byte frames; each receive frame field is reassembled across multiple short port reads so a 1024-byte frame is not truncated and desynchronised by the port read timeout; the end-of-transfer wait is now bounded on *silence* only — a lost EOT no longer hangs a completed transfer, while a still-transmitting sender is resynchronised rather than truncated, fixing the occasional end-of-transfer hang and 1K flakiness), FR-146 (Overwrite of a host→remote upload now pre-deletes the existing remote file via `delete_remote_cmd` so receivers — notably 1K variants — that stall on an existing file cannot block the handshake). **Feature — configurable serial read timeouts:** the previously hard-coded 100 ms pyserial read timeout is now a per-port setting; the transport timeout in particular bounds each X-Modem read and was the key to resolving the 1K flakiness. **Added:** UIR-032 ("Terminal Timeout (ms)", `terminal_timeout_ms`, 10–5000, default 100), UIR-033 ("Transfer Timeout (ms)", `transport_timeout_ms`, 10–5000, default 100). **Feature — per-group configuration save:** the Serial and General Configuration dialogs' own Save buttons now persist **only** their own group of settings to the active/loaded config file (no full-store overwrite, no file-select dialog), and warn when no config file is loaded. **Added:** FR-020a, FR-021a. **Modified:** FR-014 (clarified as the full-store File > Save, in contrast to the new per-group dialog saves), DR-041 (application/SRS version now `2.13.0`). **Fix — backup host list:** the Host Files list is now refreshed immediately after the destination wipe, so the deleted files no longer linger in the pane while the backup downloads. **Fix — message consistency:** clicking **Update** (Remote Files group) with the port closed now raises the same critical error dialog as Copy to Host (new `error.terminal_not_connected`); the auto-refresh callers do not raise it. All "Terminal port not open" status/error wording was unified to "Terminal port not connected" (suffixes retained). **Code:** `terminal/xmodem.py` (1K STX send framing, CRC-first 1K receive handshake, multi-read frame-field reassembly, silence-bounded receive end); `terminal/serial_manager.py:open_port` (per-port read timeout read from config, ms→s); `utils/config_handler.py` (new `terminal_timeout_ms`/`transport_timeout_ms`/`xmodem_1k`/`recv_remote_cmd_1k`/`send_remote_cmd_1k` defaults); `gui/config_dialogs.py` (new "checkbox" field type; Serial dialog timeout fields; General dialog 1K checkbox + command fields); `app.py` (`_xmodem_1k_enabled`, `_issue_remote_cmd` 1K-command selection, `use_1k` passed to `send_file`/`receive_file`; `_erase_remote_file` Overwrite pre-delete; `_save_subset_to_active_config` per-group save + no-config warning; backup host-pane refresh after wipe; new `do_refresh_remote_files` Update-button handler). **Docs:** SRS header → 2.13.0 (date 2026-06-22); §10 feature-group rows added for the XMODEM-1K mode and the per-port serial timeouts; user manual updated for the serial timeouts. **Tests:** `tests/test_xmodem.py` (1K STX send, split-read 1K receive); `tests/test_serial_manager.py` (per-port timeout); `tests/test_config_handler.py`; `tests/test_conflict_resolution.py` (Overwrite pre-delete); `tests/test_backup_restore.py` (host-pane refresh after wipe); `tests/test_gui_smoke.py` (per-group save + no-config warning, Update-button error dialog, unified message wording). **Feature — in-app user manual (Help > Manual):** the bundled user manual is now viewable inside the application via a new **Help > Manual** item, rendered from Markdown to HTML in a non-modal, resizable viewer with working table-of-contents navigation (external links open in the host browser). **Added:** FR-023 (Help > Manual opens the Manual Window, raising an already-open one rather than duplicating it), UIR-091 (Manual Window — new §4.16), DR-047 (the manual ships as package data at `src/cpm_fm/docs/cpm_fm_manual.md`, rendered with the `markdown` library and a GitHub-compatible heading slugify — new §6.10). **Modified:** UIR-004 (Help menu now lists Manual and About). **Code:** new `gui/manual_dialog.py` (`ManualDialog`, `load_manual_markdown`, `render_manual_html`, `_github_slugify`); `app.py` (`menu_manual` handler, Help-menu Manual item + separator); `cpm_fm_manual.md` moved from the repo root into the package (`src/cpm_fm/docs/`) and its internal version header bumped to 2.13.0; all `lang/lang_*.txt` (new `menu.help.manual`, `manual.title`, `manual.load_error`, `button.close` keys); `pyproject.toml` (`markdown>=3.5` runtime dependency; `docs/*.md` package data); `_pyinstaller_common.py:build_datas` (bundle the manual at `cpm_fm/docs`) and `HIDDEN_IMPORTS` (markdown-extension modules). **Tests:** Help-menu-Manual, menu_manual open/reuse, Manual-dialog-contents, render-anchor-match, and missing-file cases in `tests/test_gui_smoke.py`. `src/version.txt` → `2.13.0`. |
| 2.12.0 | 2026-06-21 | Software Engineer | Bug-fix and Terminal-Window usability pass. **Bug — Host Update scope:** the Host Files group's **Update** button refreshed *both* file lists; it now refreshes the Host Files list only. **Modified:** FR-063 (host Update is Host-list-only), OI-14 (revised). **Code:** `app.py:setup_layout` (host Update button now calls `refresh_host_files`); the now-unused `refresh_all` helper removed. **Bug — disconnect robustness:** `do_disconnect` gated each port-close on the matching status flag, so a flag that had drifted out of sync with the real port state could leave a live comms session open. It now always attempts `close_terminal_port()`/`close_transport_port()` (safe no-ops when already closed); per-port failure dialogs, shared-port handling, and the FR-058 remote-list clear are unchanged. **Added:** OI-24. **Modified:** FR-050, FR-055 (close attempt is unconditional). **Code:** `app.py:do_disconnect`. **Improvement — UI button order:** the Backup/Restore confirmation and the drag-and-drop transfer confirmation used a `QMessageBox` whose buttons were ordered by the native platform style (affirmative left, Cancel right), violating the house convention (UIR-075). Both are now custom `QDialog`s built via a shared `_confirm_dialog` helper using `build_button_row` — **Cancel** at the far left and the affirmative button at the far right. Backup/Restore keeps its warning icon and **Continue** label (Cancel default); the drag-and-drop confirmation now shows **Cancel**/**OK** (was Yes/No). **Modified:** FR-137 (DnD confirmation is a Cancel/OK dialog obeying UIR-075). **Code:** new `app.py:_confirm_dialog`; `app.py:_on_backup_restore_confirm` and `_confirm_dnd_transfer` both delegate to it; `app.py` imports `QLabel` and `build_button_row`. **Improvement — bare <Enter>:** an empty transmit field previously sent nothing; it now transmits a bare EOL so the operator can send a lone <Enter>. **Added:** FR-155. **Improvement — control characters:** the transmit field now interprets caret notation (`^@`, `^A`–`^Z`, `^[`, `^\`, `^]`, `^_`, `^?`; `^^` = literal caret); when the field resolves to control bytes only, the EOL is suppressed so the control character is sent on its own. **Added:** FR-156. **Modified:** FR-094 (bare-control-character EOL exception), DR-041 (application/SRS version now `2.12.0`). **Code:** `gui/terminal_window.py:send_text` + new `_parse_send_text`; `app.py:handle_terminal_send` gains an `append_eol` flag (default True; programmatic callers unchanged). **Tests:** new Host-Update-scope, unconditional-disconnect, backup-confirm-order, bare-Enter, and caret-control-character cases in `tests/test_gui_smoke.py`. **Docs:** MT-H03 reworded; MT-W08/MT-W09 added (manual test plan + scorecard). `src/version.txt` → `2.12.0`. |
| 2.11.2 | 2026-06-20 | Software Engineer | Fixed a defect where saving the General Configuration Dialog reverted the active session host directory. With a host directory stored in the loaded configuration and a *different* directory currently selected via Change Directory (FR-062), opening the General Config dialog and saving an unrelated general setting forced the active host directory back to the stored value, because the save callback re-applied the dialog's "Default Host Directory" field whenever it was non-empty — and that field always carries the stored value back when the user leaves it untouched. **Modified:** UIR-053 (the active session host directory now follows the field on Save only when the field's value differs from the value it was seeded with; an untouched field disturbs neither the active session directory nor the stored config value), DR-041 (application/SRS version now `2.11.2`). **Code changes:** `app.py:menu_general_config` (`update_settings` captures the stored `host_directory` before `settings.update`, then applies the field to `self.host_dir`/`refresh_host_files` only when the returned value is non-empty *and* differs from the stored value); `src/version.txt` → `2.11.2`; new `test_general_config_save_keeps_current_host_dir` case in `tests/test_gui_smoke.py`. |
| 2.11.1 | 2026-06-20 | Requirements Checker | Requirements quality pass against ISO/IEC/IEEE 29148. No application behaviour changes. **Modified:** FR-072 (stale label "Refresh Host button" corrected to "Update button (Host Files group — UIR-011)", consistent with the v2.1.1 rename recorded in FR-063 and UIR-011); FR-151 (added parenthetical clarifying that the upload batch uses the listing snapshotted before the confirmation prompt, not a live re-read of the host directory after the remote wipe); FR-152 (expanded "refresh the destination listing" to explicitly cover the source (remote) drive refresh that FR-150 also requires before the Backup confirmation, and updated the follow-on sentence to reference both pane updates); FR-153 (clarified "each file" to "each regular file" and added the clause "subdirectories within the host directory are not removed" to resolve the undefined-behaviour gap; corrected the cross-reference for `delete_remote_cmd` from FR-111 (the context-menu layout requirement) to FR-117; UIR-056 (the delete-command behaviour and config-field requirements)). **Added (§1.4):** "Snapshot" definition, introduced by FR-151 without a prior glossary entry. **Structural (§3):** Renumbered the Help menu sub-section from §3.3.1 to §3.4 (it is a peer top-level menu alongside Config, not a submenu of it); all subsequent §3 sub-sections renumbered §3.4–§3.17 → §3.5–§3.18 accordingly. No requirement IDs, priorities, or verification methods were changed. **Modified:** DR-041 (version trail updated to 2.11.1). |
| 2.11.0 | 2026-06-19 | Software Engineer | Added whole-drive Backup and Restore operations. **Added:** FR-150 (Backup action — refreshes both the remote drive listing and the host directory, prompts for confirmation, deletes every host-directory file, then downloads every remote file via the existing Copy to Host batch engine; gated on both connection flags), FR-151 (Restore action — snapshots the host directory, refreshes the remote drive listing, prompts for confirmation, deletes every remote file by sending the configured `delete_remote_cmd` once per file, then uploads every host file via the existing Copy to Remote batch engine including the FR-148/FR-149 filename-validation step; gated on both connection flags), FR-152 (before any deletion the application refreshes the destination listing, then raises a modal confirmation dialog warning that ALL destination files will be deleted and re-written; Continue or Cancel; marshalled to the GUI thread via signal+event from the worker thread, NFR-004), FR-153 (on confirmation, deletes every file at the destination: host-side uses standard filesystem deletes; remote-side sends the `delete_remote_cmd` once per file, operating on the freshly-refreshed listing), FR-154 (the copy phase reuses `_transfer_to_host_batch`/`_transfer_to_remote_batch` so a single Transfer Progress Dialog serves the whole operation, transfers are sequential, Cancel is supported, each file's outcome is recorded in the history, and because the destination is emptied first no conflict prompts arise), UIR-086 (Backup toolbar action), UIR-087 (Restore toolbar action), UIR-088 (confirmation dialog titled "Confirm Backup"/"Confirm Restore" with Continue/Cancel, Cancel as default, window-manager close equivalent to Cancel). **Modified:** DR-041 (application/SRS version now `2.11.0`). **Design note:** Backup and Restore run on a worker thread (like transfers) and use the existing signal-based cross-thread model for the confirmation prompt (NFR-004). The destination-conflict detection of FR-145–FR-147 detects nothing during a Backup/Restore because the destination is emptied first; this is called out explicitly in FR-154. The source listing for Restore is snapshot from the host directory before the remote wipe, not re-read afterwards, so files added to the host directory between the wipe and the upload are excluded. The remote-side wipe (FR-153) issues individual per-file ERA commands rather than `ERA *.*` to avoid the interactive confirmation that some CP/M systems present for the wildcard form. **Code changes:** `app.py` (`do_backup`, `_backup_drive`, `do_restore`, `_restore_drive`, `_list_remote_file_names`, `_wipe_host_dir`, `_wipe_remote_drive`, `_confirm_backup_restore`, `_on_backup_restore_confirm`, `backup_restore_confirm` signal; `setup_toolbar` adds the Backup and Restore actions); all `lang/lang_*.txt` (new `toolbar.backup`, `toolbar.restore`, `dialog.backup_restore.title_backup`, `dialog.backup_restore.title_restore`, `dialog.backup_restore.body_backup`, `dialog.backup_restore.body_restore`, `button.continue` keys); `src/version.txt` → `2.11.0`; new `tests/test_backup_restore.py`. |
| 2.10.0 | 2026-06-19 | Software Engineer | Added host→remote filename validation against the CP/M 8.3 naming convention. **Added:** FR-148 (before each file in a host→remote upload batch, and before the destination-conflict check of FR-145, the application validates the file's base name against the CP/M 8.3 convention; a conforming name proceeds without prompting; the validation applies to uploads only — downloads are not validated), FR-149 (when a name is non-conforming the application prompts the user with a modal dialog naming the offending file and pre-filling an editable name field with a conforming suggestion; the user may Rename the file (the dialog validates the entered name and stays open on a non-conforming input), Skip it (recording a `skipped` history entry), or Cancel the whole batch; the renamed name is used for the PCGET launch command, the transfer-history record, and the subsequent destination-conflict check; the prompt is raised on the GUI thread from the worker thread via signal+event, NFR-004), UIR-085 (Invalid Filename Dialog — modal, titled "Invalid CP/M File Name", pre-filled editable field, Rename enabled only when the field is a valid CP/M 8.3 name with an inline error otherwise, no window-manager close control — equivalent to Cancel), DR-046 (CP/M 8.3 file-naming convention: base 1–8 chars and optional extension 0–3 chars separated by a single `.`; permitted character set excludes spaces, delimiters, wildcards, and non-ASCII; a conforming-name suggestion is derivable by upper-casing, removing disallowed characters, and truncating, falling back to `FILE` if nothing usable remains). **Modified:** FR-140 (adds `skipped` as a valid `status` value when a file is skipped due to an invalid name), FR-142 (records a `skipped` history entry when the user chooses Skip in the FR-149 prompt), DR-041 (application/SRS version now `2.10.0`). **Design note:** the validation is inserted into `_transfer_to_remote_batch` before the destination-conflict check (FR-145), so a renamed file is subject to the normal conflict-resolution flow of §3.15 — both features are invoked in order per file. The suggestion algorithm (DR-046) is intentionally lossy (it removes disallowed characters and truncates) rather than rejecting; the user sees the suggestion in the editable field and may further edit it. The `is_valid_8_3` and `suggest_8_3` helpers live in `cpm_parser.py` (the `terminal/` layer — no GUI imports, CR-014) so they are unit-testable without Qt. **Code changes:** `terminal/cpm_parser.py` (`CPMParser.is_valid_8_3`, `CPMParser.suggest_8_3`, `CPM_INVALID_CHARS`); new `gui/filename_validation_dialog.py:FilenameValidationDialog`; `app.py` (`_prompt_invalid_name`, `_on_invalid_name_detected`, `invalid_name_detected` signal; `_transfer_to_remote_batch` gains the validation call before the conflict check); all `lang/lang_*.txt` (new `dialog.invalid_name.title`, `dialog.invalid_name.body`, `dialog.invalid_name.new_name_label`, `dialog.invalid_name.inline_error` keys); `src/version.txt` → `2.10.0`; new `tests/test_filename_validation.py`. |
| 2.9.0 | 2026-06-19 | Software Engineer | Added transfer file-conflict handling — when a file being transferred already exists at the destination, the user is prompted to Overwrite, Skip, or Cancel, with an option to apply the chosen Overwrite/Skip action to all remaining conflicts in the batch. **Added:** FR-145 (before each file in a batch, the application checks for a destination conflict: for a download the check is the existence of the target path on the host filesystem; for an upload the application refreshes the remote directory listing once at the start of the batch — updating the displayed Remote Files list — and checks the file's base name upper-cased against that fresh listing; if the refresh yields no names, no conflict is detected and the upload proceeds), FR-146 (when a conflict is detected and no batch-wide policy is in effect, the application prompts the user with a modal dialog naming the conflicting file and offering Overwrite, Skip, and Cancel; Skip records a `skipped` history entry; the prompt is raised on the GUI thread from the worker thread via signal+event, NFR-004), FR-147 (the conflict prompt includes an "Apply to all remaining conflicts" checkbox; when the user ticks it, the chosen Overwrite or Skip action is stored as a batch-wide policy for all subsequent conflicts in that batch without further prompting; the policy is reset at the start of each new batch; Cancel always ends the batch immediately and is unaffected by the checkbox), UIR-084 (Transfer File-Conflict Dialog — modal, titled "File Exists", three buttons Overwrite/Skip/Cancel plus the apply-to-all checkbox, no window-manager close control — equivalent to Cancel). **Modified:** FR-140 (adds `skipped` as a valid `status` value for entries where the user declined to overwrite an existing destination file), FR-142 (records a `skipped` history entry when the user chooses Skip in the conflict prompt), UIR-083 (the Status drop-down in the Transfer History dialog now includes a "Skipped" filter option), DR-041 (application/SRS version now `2.9.0`), DR-045 (the `status` field of a history entry may now be `skipped` in addition to `success`/`failure`/`cancelled`). **Design note:** the remote-side conflict check refreshes the listing once at the start of the upload batch (not per file) to avoid sending a DIR command for every file, while still using a live listing rather than the possibly-stale cached one; the refresh also serves as the FR-099 pre-transfer refresh. The batch-wide policy is a worker-thread-local variable (reset per batch) and does not require Qt-signal marshalling. **Code changes:** `app.py` (`_destination_conflict`, `_fresh_remote_names`, `_resolve_conflict`, `_on_conflict_detected`, `conflict_detected` signal, `_conflict_policy`; `_transfer_to_remote_batch`/`_transfer_to_host_batch` gain the conflict-check and policy-resolution calls); new `gui/conflict_dialog.py:FileConflictDialog`; all `lang/lang_*.txt` (new `dialog.conflict.title`, `dialog.conflict.body`, `dialog.conflict.apply_to_all`, `button.overwrite`, `button.skip` keys; `history.status_skipped` added to the history status filter); `src/version.txt` → `2.9.0`; new `tests/test_conflict_resolution.py` and conflict-resolution cases in `tests/test_gui_smoke.py`. |
| 2.8.0 | 2026-06-18 | Software Engineer | Added a persistent file-transfer history with re-transfer (Feature 2 of the new-features plan). **Added:** FR-140 (per-attempt history entry schema: name, host-side path, direction, timestamp, status success/failure/cancelled, size, error, retry), FR-141 (JSON persistence at `~/.cpm_fm_history.json`, retention max 500 entries + 30-day age prune, graceful degradation on a missing/malformed file), FR-142 (each transfer records its per-file outcome from the worker threads via a thread-safe store), FR-143 (modal Transfer History dialog: newest-first table, filter by direction/status, export to JSON, clear with confirmation), FR-144 (re-transfer a selected entry through the existing batch flow, gated on both connection flags and the source file's existence, recorded with the `retry` flag, started only after the dialog closes), UIR-082 (toolbar **History** action), UIR-083 (Transfer History dialog layout — new §4.12), DR-045 (transfer-history file format — new §6.8). **Modified:** DR-041 (application/SRS version now `2.8.0`). **Design note:** history is recorded per file inside the existing batch workers (`_transfer_to_remote_batch`/`_transfer_to_host_batch`) — where the filename, path, size, and outcome are all known — rather than in the `_on_transfer_completed`/`_on_error_raised` slots the plan suggested, which run once per batch and carry only the direction; the per-file recording is what the schema (and accurate failure/size capture) requires. Persistence uses a dedicated home-directory JSON file (`utils/transfer_history.py`) as the plan specified, kept separate from `ConfigHandler` and `WindowState`. The store is GUI-free (CR-014) and thread-safe (a lock), so it is called directly from the transfer worker threads without Qt-signal marshalling. The remote **View** download (`_download_and_view`) is to a temp folder and is deliberately **not** recorded. **Code changes:** new `utils/transfer_history.py` (`TransferHistory` with `add_entry`/`get_entries`/`clear_history`/`export_history`/`prune_old_entries`, `default_history_path`); new `gui/transfer_history_dialog.py` (`TransferHistoryDialog`); `app.py` (`transfer_history` injectable component, `show_history`, `_retransfer`, `_record_history`, `_file_size`; `setup_toolbar` adds the History action; the two batch workers gain a `retry` flag and record each file's outcome); all `lang/lang_*.txt` (new `toolbar.history`, `history.*`, `error.history_export_failed`, `error.retransfer_file_missing` keys); `src/version.txt` → `2.8.0`; new `tests/test_transfer_history.py` and transfer-history/re-transfer/dialog cases in `tests/test_gui_smoke.py` (the shared transfer-arming helpers now inject an isolated history file so tests never touch the host's real one). |
| 2.7.0 | 2026-06-18 | Software Engineer | Added drag-and-drop file transfer (Feature 1 of the new-features plan). **Added:** FR-136 (both file panes are drag sources carrying the source pane + selected names under the private MIME type `application/x-cpmfm-file`; a drag is a copy gesture and never alters the source), FR-137 (each pane is a drop target for an internal drag from the *other* pane — Host→Remote drop = Copy to Remote, Remote→Host drop = Copy to Host; same-pane drop is a no-op; gated on both connection flags per FR-080/CR-010 and confirmed before starting; reuses the existing batch workers so FR-099/FR-105–FR-109/FR-120 behaviour is unchanged), FR-138 (the Remote pane also accepts external OS file-manager drops, treating the dropped absolute paths as Copy to Remote; the Host pane rejects external OS drops as they are not serial transfers), FR-139 (valid-drop-zone highlight while a hovering drag is acceptable, removed on leave/drop; rejected payloads do not highlight), UIR-081 (both multi-select widgets support drag-and-drop with the drop-zone highlight and confirmation dialog). **Modified:** DR-041 (application/SRS version now `2.7.0`). **Design note:** the lists migrate from plain `QListWidget` to a small `FileListWidget(QListWidget)` subclass in `gui/` that knows only its pane and how to recognise an acceptable drop; the transfer *policy* (flag check, confirmation, which batch worker on which thread) stays in `MainWindow._on_files_dropped`, preserving the signal-based cross-thread model (NFR-004) and CR-014 (the widget is in the GUI layer). The plan's per-pane "drop on host pane → transfer to remote" wording was reconciled into the intuitive cross-pane model (drop *onto* a pane = transfer *to* that pane's side); external OS drops are accepted on the Remote pane only. The proposed mode-toggle (task 1.5) was omitted as the plan itself removed it. **Code changes:** new `gui/file_list_widget.py` (`FileListWidget`, `MIME_CPM_FILES`, `decode_drop`, drag/drop event overrides, `_set_drop_active`); `app.py` (`host_list`/`remote_list` now `FileListWidget`; new `_on_files_dropped`, `_confirm_dnd_transfer`; dropped the now-unused `QListWidget`/`QAbstractItemView` imports); all `lang/lang_*.txt` (new `dialog.dnd_confirm.title`, `dialog.dnd_confirm.to_remote`, `dialog.dnd_confirm.to_host` keys); `src/version.txt` → `2.7.0`; new drag-and-drop cases in `tests/test_gui_smoke.py`. |
| 2.6.0 | 2026-06-18 | Software Engineer | Added filter and sort controls to both file-list panes (Feature 3 of the new-features plan). **Added:** FR-130 (a text filter field above each list, with a retained canonical name list so filter/sort re-apply without re-reading the source), FR-131 (filter match rules: empty = all, wildcard `*`/`?` = whole-name glob, otherwise case-insensitive substring; ~150 ms debounce), FR-132 (sort by Name or Extension, ascending/descending; case-insensitive, stable, name tie-breaker; no size sort as the CP/M `DIR` parse carries no size), FR-133 (combined filter-then-sort, shared by both panes; defaults reproduce the FR-078 display), FR-134 (per-pane filter/sort persisted in `WindowState`/`QSettings` and restored on start), FR-135 (inline clear (×) button, active-filter visual indicator, and emptying of the canonical list when a pane is cleared), UIR-079 (filter field + clear button + active indicator), UIR-080 (sort drop-down + direction arrow button). **Modified:** FR-078 (ascending order is now the default of the FR-130–FR-133 controls), DR-041 (application/SRS version now `2.6.0`). **Design note:** the lists stay as the existing `QListWidget` widgets — filter/sort are applied by re-deriving the visible rows from the canonical list — rather than migrating to a model/view architecture as the plan suggested; this delivers the same capability while preserving all existing list behaviour and tests. Persistence uses `WindowState` (not `ConfigHandler`) for consistency with the other UI/session preferences. **Code changes:** new `utils/file_filter.py` (`matches`/`has_wildcard`/`filter_names`/`sort_names`/`filter_and_sort`, pure, no GUI imports — CR-014); `gui/window_state.py` (`filter_text`/`sort_key`/`sort_descending` accessors + setters); `app.py` (`_build_filter_sort_row`, `_render_file_list`, `_apply_host_view`/`_apply_remote_view`, `_clear_remote_files`, `_persist_filter_sort`/`_restore_filter_sort`, `_update_sort_arrow`; `setup_layout` adds the filter rows; `refresh_host_files`/`_update_remote_list_ui` render through the controls; the remote-clear sites route through `_clear_remote_files`; `retranslate_ui` re-labels the sort drop-downs); all `lang/lang_*.txt` (new `main.filter_placeholder`, `main.filter_tooltip`, `main.sort.name`, `main.sort.extension`, `main.sort_by_tooltip`, `main.sort_direction_tooltip` keys); `src/version.txt` → `2.6.0`; new `tests/test_file_filter.py` and filter/sort cases in `tests/test_gui_smoke.py`. |
| 2.5.2 | 2026-06-16 | Software Engineer | Code/requirements alignment pass (`code-requirements-align` workflow). A two-way traceability audit of the SRS against the as-built `src/cpm_fm/` found no unimplemented requirements and no orphan capabilities; four Minor divergences were corrected. **Resolved OI-23 (spec-follows-code):** UIR-047/UIR-048 described the "End of Line" selector as a radio-button group, but the dialog renders it as a drop-down (same CR/LF/CRLF values, CR default). **Modified:** UIR-047, UIR-048 (reworded to the as-built drop-down), DR-041 (application/SRS version now `2.5.2`). **Code changes (alignment to existing requirements, no requirement change):** `app.py:menu_new` (FR-018/FR-121: the File > New fallback Save dialog now uses `tr("dialog.save_config.title")`/`tr("dialog.json_filter")` instead of hard-coded literals), `app.py:_recv_one_to_host` (stale comment corrected to "polls with NAK first, then 'C'" per NFR-003), `lang/lang_pirate.txt` (DR-043: fixed two mistyped keys `status.saved_lag`→`status.saved_config` and `button::save`→`button.save`, which had been silently falling back to English). **Test changes:** `tests/test_i18n.py::test_translation_keys_match_english` now parametrizes over **all** shipped languages (was `german`/`french` only), so DR-043 key-set drift is caught for every `lang_*.txt` file. `src/version.txt` → `2.5.2`. |
| 2.5.1 | 2026-06-16 | Software Engineer | Unit-test quality pass (no application behaviour change). Strengthened and extended the automated test suite (117 → 165 tests) and recorded the new test traceability in §9 Source columns. **No requirements added or modified** except the version trail. **Modified:** DR-041 (application/SRS version now `2.5.1`). **Traceability added (Source columns):** NFR-003, FR-081, FR-082, FR-083 → `tests/test_xmodem.py` (new independent CRC-16/XMODEM and checksum vectors that no longer self-reference the packet builder, plus NAK-exhaustion, missing-file, no-start-char, EOF-padding, CRC-mode receive, STX/1024-byte (XMODEM-1K) frames, duplicate-packet re-ACK, corrupt-trailer recovery, and empty-transfer paths); FR-011, FR-012, FR-014, IFR-004 → new `tests/test_config_handler.py` (valid load, missing-file and malformed-JSON degrade-to-`{}`, save round-trip, `OSError`→`False`); FR-030, FR-036, FR-050, FR-052, FR-055, FR-057, FR-091, FR-096, NFR-001, NFR-002 → extended `tests/test_serial_manager.py` (parity map + unknown→NONE, numeric coercion, nested-key fallbacks, open-failure, `send_data`, `close_terminal_port`/`close_transport_port` success+failure, `_read_loop` dispatch and pause/resume); FR-077, FR-101, DR-033 and a §6 section-level note → `tests/test_cpm_parser.py` (multi-token base join, lowercase drive, empty mid-entry, dot-only bar entry, de-duplication, CRLF, empty input; case-insensitive prompt split into two single-assertion tests). **Code changes:** none to `src/` (the audit's deliberate mutation probes were reverted); new `tests/test_config_handler.py`, additions to `tests/test_xmodem.py`/`test_serial_manager.py`/`test_cpm_parser.py`; `src/version.txt` → `2.5.1`. |
| 2.5.0 | 2026-06-15 | Software Engineer | Added a branded application icon. **Added:** UIR-078 (a branded application icon set centrally on the `QApplication` at start-up so every window/dialog and the OS taskbar/dock entry display it; falls back to the toolkit default if the resource is absent), DR-044 (the runtime icon ships as package data at `src/cpm_fm/icons/cpm-fm.png`, resolved relative to the package so it works from a source checkout, an installed wheel, and a one-file bundle; all icon sizes and the per-platform packaging icons are generated from the single source artwork `src/icons/cpm-fm-2.png` by `tools/make_icons.py`). **Modified:** CR-006 (now records the as-built non-Python-asset layout — source artwork in `src/icons/`, generated packaging icons under `assets/` including the freedesktop `hicolor/` tree, runtime icon as package data — superseding the deferred-`resources/` note from OI-16), DR-041 (application/SRS version now `2.5.0`). **Code changes:** new `tools/make_icons.py` (generates `assets/icon.ico`/`.icns`/`.png`, `assets/icons/hicolor/<size>/apps/cpm-fm.png`, and the runtime `src/cpm_fm/icons/cpm-fm.png` from `src/icons/cpm-fm-2.png`), new generated icon assets, `gui/theme.py` (`APP_ICON_PATH`, `app_icon()`), `app.py:main` (`app.setWindowIcon(app_icon())`), `_pyinstaller_common.py:build_datas` (bundle the runtime icon at `cpm_fm/icons`), `pyproject.toml` (`icons/*.png` package-data; `Pillow` added to the `dev` optional-dependencies for the icon generator), `src/version.txt` → `2.5.0`; new `test_app_icon_*` cases in `tests/test_gui_smoke.py`. |
| 2.4.0 | 2026-06-15 | Software Engineer | Surfaced the loaded config and host directory in the window chrome. **Added:** FR-125 (title bar shows the application name plus the loaded config's base name — no path, no extension — cleared by File > New), FR-126 (Host Files group title shows the current host directory, leading portion elided to fit the group width, recomputed on resize), UIR-005 (main window title-bar content). **Modified:** UIR-011 (group title now includes the host directory), DR-041 (application/SRS version now `2.4.0`). **Code changes:** `app.py` (`_update_window_title`, `_update_host_group_title`, `_config_name`; `setup_layout` stores `self.host_group`; `refresh_host_files`, `load_config`, `menu_new`, `retranslate_ui`, and new `resizeEvent` keep the titles current; `QFontMetrics` import for left-elision), all `lang/lang_*.txt` (new `app.title_with_config` and `main.host_files_dir` keys), `src/version.txt` → `2.4.0` (aligning the application version with the SRS). |
| 2.2.0 | 2026-06-14 | Software Engineer | Made the X-Modem transfer-data echo to the Terminal Window optional. **Added:** UIR-058 ("Echo Transfer Data" dropdown `OFF`/`ON`, default `OFF`, persisted as `echo_transfer_data`). **Modified:** FR-086 (the `<HH>` hex echo now occurs only when `echo_transfer_data` is affirmative — default off — independently of the FR-088 stdout trace), UIR-044 (the ungrouped general settings now include "Echo Transfer Data"), DR-041 (application/SRS version now `2.2.0`). **Code changes:** `utils/config_handler.py` (new `echo_transfer_data` default `ON`), `gui/config_dialogs.py:GeneralConfigDialog` (new dropdown field, `config.general.echo_transfer`), `app.py` (`_echo_transfer_enabled` helper; `_on_transfer_bytes` returns early when the echo is off), all `lang/lang_*.txt` (new `config.general.echo_transfer` key), `src/version.txt` → `2.2.0`; new `test_transfer_byte_echo_respects_setting` case in `tests/test_gui_smoke.py`. |
| 2.1.1 | 2026-06-14 | Software Engineer | Main-window layout tidy-up and clearer connection indicators. **Modified:** FR-063 (the both-lists refresh button was relabelled "Refresh Host" → "Update" and moved from beneath the Host Files list to beside "Change Directory"; behaviour unchanged), UIR-011 (Host Files group now has a top row with "Change Directory" and "Update" sized equally; the lower row holds only "Copy to Remote"), UIR-012 (the Remote Files drive drop-down and "Update" button are sized equally), UIR-017 (drop-down sized equally with the Update button), UIR-074 (not-connected indicator colour changed from grey to red), DR-041 (application/SRS version now `2.1.1`). **Code changes:** `app.py:setup_layout` (host top row with equal stretch; drive drop-down and Update button equal stretch, stretch spacer removed), `app.py:_update_indicators` (not-connected colour `#9e9e9e` → `#f44336`); `lang/lang_english.txt`/`lang_german.txt`/`lang_french.txt` (host button now reuses `main.update`; the now-dead `main.refresh_host` key removed from all three files); `src/version.txt` → `2.1.1`. |
| 2.1.0 | 2026-06-14 | Software Engineer | Regrouped the General Configuration Dialog: the remote-command fields are now gathered into a "Remote" group placed first, and the Rename/Delete fields are relabelled without the "Remote" suffix (group membership conveys it). **Modified:** UIR-041 ("Remote" group, first, containing List Files, Receive from Remote, Send to Remote, Rename, Delete — replaces the former "Terminal Commands" group), UIR-044 (the remaining general settings presented below, ungrouped — replaces the former "Xmodem Commands" group), UIR-055/UIR-056 (relabelled "Rename"/"Delete", moved into the Remote group), DR-041 (application/SRS version now `2.1.0`). **Code changes:** `gui/config_dialogs.py` (fields gain an optional `group` key; `create_widgets` lays fields into per-group `QGroupBox` sections in first-appearance order; field-building extracted to `_build_field`; `GeneralConfigDialog` field list reordered with the five remote fields first carrying the `config.general.remote_group` group); `lang/lang_english.txt`/`lang_german.txt`/`lang_french.txt` (new `config.general.remote_group` key; `rename_remote`/`delete_remote` labels shortened); `src/version.txt` → `2.1.0`; new `test_general_config_remote_group_first` case in `tests/test_gui_smoke.py`. |
| 2.0.0 | 2026-06-14 | Software Engineer | Added internationalisation (multi-language GUI). **Added:** FR-121 (all user-facing GUI strings externalised to language files and resolved at run time by placeholder key; semantic/protocol values excluded), FR-122 (Config > Language submenu lists every available language by name; selecting one sets the active language), FR-123 (changing language re-translates the visible UI live, no restart), FR-124 (active language persisted/restored; default and fallback English; missing-key fallback English→key), UIR-077 (Language submenu — new §4.11 — checkable, mutually-exclusive, language names not translated, active one checked), DR-042 (language files: `src/cpm_fm/lang/lang_<language>.txt`, UTF-8, `key = value` split on first `=`, `#`/blank lines ignored, `str.format` placeholder values, shipped as package data — new §6.6), DR-043 (English is the complete reference/fallback language; shipped files define the same keys), CR-015 (only human-facing prose is translated, not option values / drive letters / command templates), NFR-005 (a new language needs only a new `lang_*.txt` file, no code change). **Modified:** UIR-003 (Config menu now also lists Language), DR-041 (application/SRS version now `2.0.0`), CR-014 (the translator `utils/i18n.py` observes the no-GUI-import rule). **Code changes:** new `utils/i18n.py` (`tr`, `set_language`/`current_language`, `available_languages`/`display_name`, `_parse`); new `lang/lang_english.txt` (reference) plus `lang_german.txt`/`lang_french.txt` stubs; `gui/window_state.py` (`language` property); `app.py` (set language before building widgets, `_register_text`/`retranslate_ui` registry, `_add_menu_action`, `_setup_language_menu`/`menu_set_language`, indicators keyed by i18n key, all literals → `tr()`); `gui/terminal_window.py`, `gui/config_dialogs.py` (field `label_key`), `gui/file_action_dialog.py`, `gui/transfer_dialog.py`, `gui/about_dialog.py` (strings → `tr()`); `pyproject.toml` (`lang/*.txt` package-data); `src/version.txt` → `2.0.0`; new `tests/test_i18n.py` and Language/retranslate cases in `tests/test_gui_smoke.py`. |
| 1.10.0 | 2026-06-14 | Software Engineer | Added program versioning and an About dialog. **Added:** DR-040 (the version number is stored in a plain-text `src/version.txt` containing a single semantic-version string, read by the application), DR-041 (the application version matches the SRS version; fall back to `0.0.0` if the file is unreadable), FR-022 (Help > About presents the modal About Dialog), UIR-004 (Help menu with an About item), UIR-076 (About Dialog — new §4.10 — program name "CP/M File Manager", "Version `<x.y.z>`", a clickable GitHub-repo hyperlink opening in the default browser, and a centred OK button). **Modified:** UIR-075 (now also covers the About Dialog's single OK button). **Code changes:** new `src/version.txt` (now `1.10.0`); new `version.py` (`APP_NAME`, `REPO_URL`, `get_version` reading `version.txt`); `__init__.py` (`__version__` derived from `get_version`); new `gui/about_dialog.py:AboutDialog`; `app.py` (`setup_menu` adds the Help menu, `menu_about` handler). |
| 1.9.0 | 2026-06-14 | Software Engineer | Added the ability to cancel an in-progress file transfer. **Added:** FR-120 (Cancel button on the progress dialog requests cancellation; the active X-Modem transfer is signalled to abort and transmits the CAN sequence so the remote aborts too; the batch stops, the dialog closes, the status shows a cancellation message, and the destination list refreshes if any file already completed; a partially-received file is not written). **Modified:** UIR-051 (centred Cancel button, disabled/"Cancelling…" on request), NFR-003 (CAN-0x18 abort on cancel). **Code changes:** `terminal/xmodem.py` (`CAN`, `cancel_check` callable, `_cancelled`/`_abort`, cancel checks in `send_file`/`receive_file` and the read waits), `gui/transfer_dialog.py` (Cancel button + `cancel_callback` + `mark_cancelling`), `app.py` (`_transfer_cancel` `threading.Event`, `_request_transfer_cancel`, `transfer_cancelled` signal + `_on_transfer_cancelled`, batch-driver cancel handling, `cancel_check` passed to `XModem`). |
| 1.8.2 | 2026-06-14 | Software Engineer | Standardised dialog button layout across all dialogs. **Added:** UIR-075 (new §4.9 — when both buttons are present Cancel sits at the far left and the affirmative button at the far right with a stretch between; a lone button is centred). **Modified:** UIR-057 (File Action Dialog buttons laid out per UIR-075). **Code changes:** new `gui/dialog_buttons.py:build_button_row` shared helper; `gui/config_dialogs.py` and `gui/file_action_dialog.py` now build their button rows via the helper (replacing the platform-ordered `QDialogButtonBox`), keeping their existing labels (Save/Cancel and Apply/Cancel). |
| 1.8.1 | 2026-06-14 | Software Engineer | Extended the v1.8 context menus with one-click transfer shortcuts. **Added:** FR-119 (a **To Remote** action on the Host Files context menu and a **To Host** action on the Remote Files context menu, each transferring the single file under the cursor, behaving as the corresponding Copy to Remote / Copy to Host button — both-flag guard FR-080/CR-010, progress dialog FR-105, destination refresh FR-099). **Modified:** FR-110/FR-111 (menus now include the transfer action), UIR-018/UIR-019 (transfer item at the top of each menu, separated from the file actions). **Code changes:** `app.py` (`_host_to_remote`/`_remote_to_host` handlers reusing `_transfer_to_remote_batch`/`_transfer_to_host_batch`; menu builders gain the new items + a separator). |
| 1.8.0 | 2026-06-14 | Software Engineer | Added right-click file-management actions to both file panes. **Added:** FR-110/FR-111 (Host/Remote context menus — View/Edit·Rename·Delete and View·Rename·Delete), FR-112 (open a file in the configured viewer/editor `viewer_cmd`, with OS-default fallback), FR-113 (remote View downloads the file to a temp folder over X-Modem first, then opens it), FR-114/FR-115 (modal File Action Dialog for Rename/Delete: editable field for Rename, read-only for Delete; Apply/Cancel), FR-116 (host Rename/Delete via standard filesystem operations), FR-117 (remote Rename/Delete via configured `rename_remote_cmd` default `REN $2=$1` and `delete_remote_cmd` default `ERA $1`, sent on the Terminal Port), FR-118 (refresh the affected list after a successful action), UIR-018/UIR-019 (the two context menus), UIR-054/UIR-055/UIR-056 (Viewer/Editor, Rename Remote, Delete Remote fields in the General Config dialog), UIR-057 (File Action Dialog, new §4.8). **Code changes:** `utils/config_handler.py` (`viewer_cmd`/`rename_remote_cmd`/`delete_remote_cmd` defaults), `gui/config_dialogs.py` (three new General-config fields), new `gui/file_action_dialog.py:FileActionDialog`, `app.py` (context-menu policies and builders, `_host_view`/`_host_rename`/`_host_delete`, `_remote_view`/`_remote_rename`/`_remote_delete`, `_download_and_view`/`_on_view_file_ready`, `_do_remote_file_cmd`, `_open_in_viewer`/`build_viewer_args`/`_os_open`, `view_file_ready` signal). Remote-action UI updates are marshalled to the GUI thread via signals (NFR-004). |
| 1.7.3 | 2026-06-13 | Software Engineer | Fixed a defect where the Update button (Remote Files group) listed the remote's then-current drive instead of the drive shown in the drive-selection drop-down. If the operator changed the remote drive directly in the Terminal Window, Update returned the wrong drive's files. **Resolved OI-22 (code-follows-spec):** `refresh_remote_files` now switches to the displayed drive first (running `_do_change_drive_logic`), behaving exactly as if that drive had just been selected (FR-100–FR-104). **Modified:** FR-073. **Code changes:** `app.py:refresh_remote_files`. |
| 1.7.2 | 2026-06-13 | Software Engineer | Added persistence of the last-used configuration-file folder, kept separate from the Host Files directory. The File > Load and File > Save dialogs now reopen in the folder where a config was last loaded/saved. **Added:** FR-006 (remember the config-file folder, `QSettings`-backed, distinct from the host directory FR-060). **Modified:** FR-010, FR-013 (dialogs default to the remembered config folder). **Code changes:** `window_state.py` (`last_config_dir` property), `app.py` (`menu_load`/`menu_save` seed and update it). |
| 1.7.1 | 2026-06-13 | Requirements Checker | Code/spec alignment pass against the as-built source. **Resolved OI-20 (code-follows-spec):** the Flow Control setting (UIR-028), previously collected but never applied and not listed as deferred, is now applied at port open — `serial_manager.py:open_port` maps `flow`/`flow_control` onto pyserial's `xonxoff`/`rtscts`/`dsrdtr` (NFR-002 key-name fallback); added `tests/test_serial_manager.py`. **Modified:** UIR-028 (setting is applied). **Resolved OI-21 (spec-follows-code):** FR-075/FR-100 said the list/drive command is sent "to the receive text area", but the app echoes it there only when Local Echo is on; otherwise it appears via the remote's serial echo. **Modified:** FR-075, FR-100 (reworded to the as-built echo behaviour). |
| 1.7.0 | 2026-06-12 | Software Engineer | Added support for persisting the host directory per configuration file. **Modified:** FR-060 (startup directory logic), FR-062 (persistence timing). **Added:** UIR-053 (Default Host Directory field in General Config Dialog). |
| 1.6.1 | 2026-06-08 | Requirements Checker | Fixed a multi-file-batch timing defect: the leading characters of the second and subsequent launch commands (e.g. `PCPUT $1`) were lost because the command was sent while CP/M was still returning to the CCP prompt after the previous transfer and not yet servicing its UART, so the remote reported "command not found" and the transfer failed. **Added:** FR-109 (wait for the terminal prompt to return, plus a settle delay, before each launch command after the first), UIR-052 ("Xfer Inter-file Delay (s)" field, default 2). **Code changes:** `app.py` (`_wait_for_terminal_idle`, `_interfile_delay`, called between files in `_transfer_to_remote_batch`/`_transfer_to_host_batch`), `gui/config_dialogs.py` (new field). Also fixed a related receive-handshake defect exposed once the launch command stopped being truncated: the X-Modem receiver polled with the CRC start character `'C'` first, which the checksum-only CP/M senders (PCPUT V1.0) reject with "Unknown response from host". **Modified:** NFR-003 (receive polls NAK before `'C'`). **Code changes:** `terminal/xmodem.py:receive_file`. |
| 1.6 | 2026-06-08 | Requirements Checker | Added multi-file transfer. The Copy to Remote / Copy to Host actions now transfer every file selected in the (already multi-select) list. **Added:** FR-106 (transfer all selected files; warn if none selected), FR-107 (sequential transfer in list order, one X-Modem session per file), FR-108 (abort the batch on the first failure, naming the failed file; refresh once if any succeeded). **Modified:** FR-105 (single dialog serves the whole batch, showing "File `i` of `N`"), UIR-051 (batch-position label). **Code changes:** `gui/transfer_dialog.py` (constructor takes a file count, `batch_label`, `set_file`), `app.py` (`_selected_filenames`, `_send_one_to_remote`/`_recv_one_to_host` per-file helpers, `_transfer_to_remote_batch`/`_transfer_to_host_batch` drivers, `batch_started`/`transfer_file_started` signals replacing `transfer_started`). |
| 1.5 | 2026-06-08 | Requirements Checker | Added a transfer-progress dialog shown for the duration of every X-Modem transfer. **Added:** FR-105 (modal progress dialog: filename + cumulative blocks/bytes, updated per block, auto-closed on success or failure), UIR-051 (dialog contents/layout, new §4.7). **Code changes:** `xmodem.py` (per-packet `progress` hook in `send_file`/`receive_file`), new `gui/transfer_dialog.py:TransferProgressDialog`, `app.py` (`transfer_started`/`transfer_progress` signals, `_on_transfer_started`/`_on_transfer_progress`/`_close_transfer_dialog`, `_on_transfer_progress_cb`, dialog teardown in `_on_transfer_completed`/`_on_error_raised`). Progress is marshalled worker→GUI thread via Qt signals (NFR-004). |
| 1.4 | 2026-06-06 | Requirements Checker | Updated GUI layout requirements to reflect the move of transfer and refresh buttons from the toolbar to rows beneath their respective file lists. Renamed 'Refresh' to 'Refresh Host'. Affected IDs: FR-063, FR-072, UIR-011, UIR-012, UIR-013, UIR-071. |
| 1.3 | 2026-06-06 | UI Migration | Specified migration of the GUI from Tkinter to **PySide6 (Qt for Python)** with a Material Design theme, per stakeholder decision (framework: PySide6; theme: `qt-material`; default theme mode: follow OS; full requirement traceability requested). **Added:** UIR-070 (Material theme), UIR-071 (top toolbar), UIR-072 (draggable splitter between panes), UIR-073 (OS-following light/dark default, dark fallback), UIR-074 (status-bar connection indicators, Desirable), CR-012 (PySide6 mandated, no Tkinter), CR-013 (`qt-material` theme applied centrally at start-up), CR-014 (GUI/`terminal`/`utils` decoupling — no GUI imports in non-GUI layers), NFR-004 (Qt signal/slot thread-safety). **Modified:** §1.2 Scope and §1.4 Definitions (Qt/PySide6/QSS/Material added), STR-002 (PySide6 noted), UIR-013 (actions presented as a toolbar from v1.3), NFR-001 (cross-thread GUI updates via Qt signals/slots, replacing `self.after`). The X-Modem, serial-management, CP/M parsing, and configuration layers (`terminal/`, `utils/`) and all of §3 functional behaviour are unchanged by this migration. Status remains Reviewed; the new requirements are approved for implementation but not yet implemented. |
| 1.2 | 2026-06-06 | Requirements Checker | Reconciled code/spec discrepancies found by a multi-agent requirements review, per stakeholder decisions (OI-09–OI-17). **Added:** FR-063 (Refresh refreshes both lists), FR-086 (X-Modem transfer bytes echoed to the Terminal Window as hex `<HH>` tokens). **Removed:** FR-035 (Connect no longer opens the Terminal Window; superseded by FR-097). **Modified:** FR-011/FR-012 (Load is a full replace; unrecognised keys retained but inert), FR-073 (Update is Remote-list-only), FR-076 (≥1 s wait then 0.5 s buffer idle-timeout, max 10 s), FR-080/CR-010 (transfer requires **both** status flags), FR-084/FR-085 (removed stale "empty stub" notes), NFR-003 (final packet padded to 128 bytes with 0x1A), CR-002 (`__main__.py` entry point), CR-006 (resources folder deferred, Desirable), CR-007/CR-008 (relaxed to as-built cohesive-module / snake_case naming). Also resolved OI-18 (implemented the FR-050–FR-057 disconnect sequence with per-port failure dialogs and workflow cancellation) and OI-19 (implemented real receive/transmit data buffers cleared by the Clear button); **modified** FR-090/FR-092 (concrete buffers) and FR-095 (clears buffers too). **Code changes:** `app.py` (both-flag transfer guard, buffer idle-timeout, `refresh_all`, `_on_transfer_bytes` hex echo, full `do_disconnect` sequence, `_rx_buffer`/`_tx_buffer` + `clear_terminal_buffers`), `xmodem.py` (final-packet padding, monitor hook on all reads/writes), `serial_manager.py` (`close_terminal_port`/`close_transport_port`), `terminal_window.py` (Clear `clear_callback`). All v1.2 issues (OI-09–OI-19) closed. Updated §9 traceability and §10 issue log. |
| 1.1 | 2026-06-06 | Requirements Checker | Resolved all open issues (OI-01–OI-08) by inspecting the implementation under `src/cpm_fm/`. **Added:** UIR-016 (Disconnect button), FR-097 (Terminal button behaviour), FR-098 (send-with-port-closed status), CR-011 (deferred settings). **Modified:** UIR-013 (added Disconnect to button set), UIR-014 (127-char truncation behaviour), UIR-030/UIR-031 (transmit-delay stored-only note), UIR-043 (Change Disk stored-only note), FR-078 (sorted remote-list display), CR-010 (as-built transfer guarding vs. stub spec), NFR-001 (measurable threading criteria), NFR-002 (key-name normalisation detail), NFR-003 (SOH/checksum X-Modem detail). Converted §10 from an open-issues list to a closed issue-resolution log. Updated §9 traceability. Status advanced from Draft to Reviewed. |
| 1.0 | 2026-06-06 | Requirements Checker | Initial baseline. Consolidated and restructured all requirements from `docs/App_Requirements.md` and `docs/App_Design.md` into an ISO/IEC/IEEE 29148-conformant SRS. Assigned unique IDs across STR/FR/UIR/IFR/DR/CR/NFR categories (STR-001–003, FR-001–096, UIR-001–067, IFR-001–004, DR-001–032, CR-001–010, NFR-001–003). Added verification methods, priorities, source traceability, a traceability summary (§9), and an open-issues log (§10, OI-01–OI-08). |
